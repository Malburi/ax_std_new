---
name: vibe
description: 게이트 없이 AI가 알아서 바로 작업하는 모드. "알아서 해줘", "그냥 해줘", "바이브로", "바이브 코딩", "빠르게 그냥 고쳐", "vibe" 요청 시 트리거. 영향도 분석·안전성 평가를 생략하고 즉시 수정한다.
---

# Vibe (알아서 모드)

analyze-impact / change-safety 게이트를 **생략**하고 요청을 바로 처리한다.

## 규칙
1. **외과적 변경 원칙 유지** — 요청된 부분만 건드린다. 인접 코드·주석·포맷 "개선" 금지.
2. `.claude/patterns/*.md` 가 있으면 가볍게 참고한다 (강제 아님 — 로드 실패해도 진행).
3. 완료 후 변경 파일 목록을 한 줄로 보고한다.

## 승격 조건
아래에 해당하면 바로 진행하지 말고 safe-modify 로 승격을 제안한다.
- DB 스키마 변경 (DDL, 컬럼 추가/삭제)
- 외부 API 계약 변경 (요청/응답 필드, 엔드포인트)
- 트랜잭션 경계 변경
- 3개 이상 파일에 걸친 수정

기본 원칙: 범용 수정/개발 요청("고쳐줘", "개발해줘")은 safe-modify / scaffold-feature 가 기본 경로이고, 이 스킬은 사용자가 명시적으로 "알아서/그냥/바이브"라고 말할 때만 탄다.
