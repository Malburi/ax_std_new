# CLAUDE.md

> 💡 **토큰 비용 절감 꿀팁 (바이브 코딩 시 필수)**
> - **코드 수정 후 전체 재분석 금지**: 전체 재초기화 대신 `"인덱스만 갱신해줘"`라고 요청하면 변경된 파일만 부분 증분(Incremental) 분석합니다.
> - **부분 단계 실행**: 특정 요소만 갱신할 때는 `"스킬만 다시 생성"`, `"패턴만 다시"`, `"validator만 실행"` 등으로 필요한 단계만 핀포인트 요청하세요.
> - **경량 분석 모드 (Standard)**: 토큰 소비를 줄이려면 하네스 초기화 시 `"빠르게 하네스 구축해줘"` 문구를 포함하면 Standard Tier로 실행합니다 (기본은 Full, Lite Tier는 폐지됨).

## 보편 에이전트 행동 원칙

> Andrej Karpathy 가이드라인 기반. 모든 에이전트·스킬에 공통 적용.

### 1. 코딩 전 사고

구현 전 반드시.

- **가정 명시**: 불확실한 사항은 숨기지 않고 사용자에게 알린다.
- **해석 충돌**: 복수 해석이 가능하면 모두 제시하고 선택을 구한다.
- **단순 대안**: 더 간단한 방법이 있으면 먼저 제안한다. 무조건 실행하지 않는다.
- **모호함 정지**: 요청이 불명확하면 멈추고 이름을 붙여 질문한다.

### 2. 단순성 우선

요청된 것만 만든다.

- 요청받지 않은 기능·추상화·유연성은 추가하지 않는다.
- 새 에이전트·스킬 추가 전 자문: "기존 에이전트로 해결되는가?"
- 200줄로 해결 가능한데 50줄로 되면 50줄로 쓴다.

### 3. 외과적 변경

요청된 부분만 건드린다.

- 인접 코드·주석·포맷을 "개선"하지 않는다.
- 내 변경이 만든 미사용 import/변수/함수만 제거한다.
- 기존 dead code는 언급하되 삭제하지 않는다.
- 변경된 모든 줄은 사용자 요청에 직접 연결되어야 한다.

### 4. 목표 기반 실행

→ `_workspace/` 파이프라인(Phase 2-1~2-5 + Phase 4 harness-evaluator)으로 구현됨.

### 5. 한국어 출력 규칙

한국어 문장은 마침표(`.`), 물음표(`?`), 느낌표(`!`)로 끝낸다.  
목록이나 예시가 이어져도 문장 종결에 콜론(`:`)을 쓰지 않는다.  
코드·키-값 쌍·레이블 내부의 콜론은 허용.

### 6. 새 파일 헤더 주석

새 `.py` 파일 첫 줄: `# 이 파일의 역할을 한 줄 한국어로`  
`agents/*.md` 에이전트: frontmatter `description` 필드가 헤더 역할을 대신함.  
config / template / requirements 파일은 헤더 생략.

### 7. 계획 + 체크리스트 + 컨텍스트

→ `_workspace/` 파일 체계(writer_decisions.json · CLAUDE.md 변경이력)로 구현됨.

### 8. 완료 전 테스트 실행

→ `validator_checks.py` + `qa_boundary6.py` + `harness-evaluator` 루프로 구현됨.

### 9. 의미 단위 커밋

- 하나의 논리적 변경이 완료되면 즉시 커밋한다. 사용자 요청 전에.
- 커밋 메시지는 한 문장으로 설명 가능한 단위만.
- 좋은 예: `"change-safety: opus→sonnet 모델 수정"`
- 나쁜 예: `"에이전트 4개 수정 + 버그 수정 + 문서 업데이트"` (3개로 분리)
- 파트너 저장소 커밋은 자동 실행하지 않는다. 반드시 확인 후.

### 10. 에러 읽기, 추측 금지

- 전체 에러 메시지와 스택 트레이스를 읽는다. 키워드만 보지 않는다.
- 기억 기반의 "흔한 수정"을 확인 없이 적용하지 않는다.
- 원인 불명 시: print/log를 추가해 상태를 확인한 후 수정한다.
- WARN 후 폴백한 경우 폴백 사유를 `_workspace/`에 기록한다.

---

**harness-fin** — ITO/SI 조직을 위한 확장 메타 하네스 템플릿.

## 이 저장소의 역할

`.claude/` 폴더에 *코드베이스 분석 + 수정/개발/마이그레이션 작업까지* 지원하는 에이전트 팀과 워크플로우 스킬이 포함되어 있다.

대상 프로젝트의 코드베이스를 분석해 맞춤형 CLAUDE.md / 워크플로우 스킬 / 도메인 에이전트 / 패턴 / 인덱스를 한 번에 생성하고, 이후 *수정·개발·마이그레이션 작업*까지 끊김 없이 지원한다.

[neoruler001/harness-new](https://github.com/neoruler001/harness-new)의 4-에이전트 파이프라인을 기반으로 [revfactory/harness](https://github.com/revfactory/harness)의 메타 방법론을 확장 적용했다.

## 실행 모드

**에이전트 팀** — `TaskCreate` 의존성 + `_workspace/` 파일 기반 산출물 전달

**팀 구성:**
- 분석/생성 파이프라인: `analyzer` → `writer` → (`pattern-extractor`) → `validator` → `qa`
- 품질 루프: `spec-clarifier` (Phase -1, 사전 명세화) + `harness-evaluator` (Phase 4, 사후 eval)
- 작업용 에이전트: `impact-analyzer`, `change-safety`, `migration-planner`, `test-generator`, `sql-reviewer`, `legacy-decoder`, `doc-syncer`, `logic-tracer`, `feature-finder`, `api-bridge`
- 크로스 리포 에이전트: `api-bridge` (백엔드↔프론트엔드 API 계약 추출·검증·스텁 생성)

## 파일 구조

플러그인 표준 레이아웃 — `agents/`는 flat, `skills/`는 폴더/`SKILL.md`.

| 경로 | 역할 |
|------|------|
| `.claude-plugin/marketplace.json` | 마켓플레이스 카탈로그 (단일 저장소 = 단일 플러그인) |
| `.claude-plugin/plugin.json` | 플러그인 매니페스트 |
| `skills/harness-init/SKILL.md` | 메인 오케스트레이터 (분석 → 생성 → 검증 → QA → 패턴) |
| `skills/analyze-impact/SKILL.md` | 영향도 분석 워크플로우 |
| `skills/safe-modify/SKILL.md` | 안전 변경 워크플로우 (사전 영향 + 사후 안전성) |
| `skills/scaffold-feature/SKILL.md` | 컨벤션 기반 신규 기능 스캐폴딩 |
| `skills/vibe/SKILL.md` | 게이트 생략 알아서 모드 ("알아서 해줘" — 사소한 변경 즉시 처리, 위험 시 safe-modify 승격 제안) |
| `skills/plan-migration/SKILL.md` | 마이그레이션 계획 워크플로우 |
| `skills/review-sql/SKILL.md` | SQL 종합 리뷰 워크플로우 |
| `skills/spec-gate/SKILL.md` | 작업 전 소크라테스식 명세 명확화 워크플로우 (Ouroboros 영감) |
| `skills/harness-clean/SKILL.md` | harness 전체 제거 워크플로우 (확인 후 안전 삭제) |
| `skills/trace-logic/SKILL.md` | 기능·API·화면 처리 흐름 추적 워크플로우 |
| `skills/find-feature/SKILL.md` | 기능명·키워드로 관련 코드 위치 탐색 워크플로우 |
| `skills/generate-wiki/SKILL.md` | harness 산출물 → wiki 페이지 세트 생성 (call_graph.json → vis-network 인터랙티브 HTML) |
| `skills/pair-init/SKILL.md` | 별도 저장소 백엔드·프론트엔드 연동 (pair_config.md 생성 + API 계약 추출 + 드리프트 검증) |
| `skills/cross-repo-scaffold/SKILL.md` | 전체 스택 기능 동시 스캐폴딩 (백엔드 레이어 + 프론트엔드 서비스·컴포넌트·라우트) |
| `skills/cross-repo-modify/SKILL.md` | 페어 연동된 양쪽 저장소에 기존 기능 개선/수정 동시 반영 (safe-modify + 파트너 영향 확인·반영 게이트) |
| `skills/publish-wiki/SKILL.md` | 폴더 wiki를 별도 프로젝트 wiki-hub(중앙 DB, 시스템·컴포넌트·버전관리)에 발행 — `wiki-hub-publish` 콘솔 명령 호출 |
| `skills/wiki-hub/SKILL.md` | 별도 프로젝트 wiki-hub 실행 — 여러 시스템 wiki 통합 열람·검색·버전 이력·되돌리기 (`wiki-hub-serve` 콘솔 명령 호출) |
| `agents/spec-clarifier.md` | Phase -1: 소크라테스 인터뷰 + 모호성 점수화 + 명세 리포트 생성 |
| `agents/harness-evaluator.md` | Phase 4: 4차원 품질 평가 (커버리지·정확도·실행가능성·컨텍스트) + fix_targets 반환 |
| `agents/analyzer.md` | Phase 2-1: 심층 분석 (스택 + 의존성 그래프 + 데이터 흐름 + 트랜잭션 + 외부 통신 + 인덱스 생성) |
| `agents/writer.md` | Phase 2-2: 하네스 파일 + 워크플로우 스킬 생성 |
| `agents/pattern-extractor.md` | Phase 2-2.5: 컨벤션 추출 (writer 직후) |
| `agents/validator.md` | Phase 2-3: 구조 검증 + 인덱스 무결성 |
| `agents/qa.md` | Phase 2-4: 경계면 교차 비교 (Boundary 1~6) |
| `agents/impact-analyzer.md` | 변경 영향도 분석 |
| `agents/change-safety.md` | 변경 안전성 평가 (GO/HOLD/STOP) |
| `agents/migration-planner.md` | 스택 마이그레이션 계획 |
| `agents/test-generator.md` | 회귀 테스트 골격 생성 |
| `agents/sql-reviewer.md` | SQL 다각도 리뷰 |
| `agents/legacy-decoder.md` | 레거시 코드 역공학 |
| `agents/doc-syncer.md` | 코드 ↔ 문서 동기화 점검 |
| `agents/logic-tracer.md` | 기능·API·화면 처리 흐름을 진입점 → Controller → Service → DB까지 추적 |
| `agents/feature-finder.md` | 기능명·키워드로 관련 파일·클래스·메서드·SQL 위치 탐색 |
| `agents/api-bridge.md` | REST API 계약 추출(extract)·드리프트 검증(validate)·프론트 스텁 생성(generate-stub)·파트너 영향 확인(check-impact) |
| `agents/lib/wiki_generator.py` + `agents/lib/wiki_content.py` | harness 산출물을 그대로 wiki 페이지로 변환(zero-LLM) + call_graph.json → vis-network 인터랙티브 HTML |
| `agents/lib/wikihub_db/` (models/store/config/index_extract/publish.py) | wiki DB 발행(쓰기) — 별도 프로젝트 wiki-hub의 스키마·저장 로직을 그대로 옮긴 사본(view 전용 server/ui/render는 제외). wiki-hub 설치 없이 harness가 직접 DB에 씀 |
| `agents/lib/build-index.mjs` | 결정론적 전수 인덱서(Node 18+, npm 의존성 0) — `_workspace/index/`의 symbols·call_graph·sql_usage·transactions·external_io·env_branches·schema·api_contract·dead_code + `_meta`·`_analysis_input`·`_unresolved`를 LLM 없이 생성. upstream AX-Harness에서 이식 후 이 저장소 계약에 맞게 패치 |
| `agents/lib/ai-budget.mjs` | harness-init Phase 2(analyzer/writer/pattern-extractor)의 AI 호출을 role당 initial 1회로 스크립트가 강제하는 예산 게이트. upstream 이식, 거의 무수정 |
| `agents/lib/validate-harness.mjs` | `_workspace/index/*.json`을 `docs/index-schema/*.json` 대조 JSON 스키마로 검증(형태 검증) — `validator_checks.py`의 check7/7b(내용 정확성)와 병행. upstream 이식 후 analyzer.md 섹션 체계와 안 맞는 마크다운 프로즈 검사는 제거 |
| `agents/lib/tests/` | `build-index.mjs`/`ai-budget.mjs`/`validate-harness.mjs` 회귀 테스트 24종 + 무의존 러너 (`node agents/lib/tests/run.js`) |
| `agents/lib/analyzer_index_summary.py` | analyzer 리포트 Section B/D(의존성그래프·트랜잭션·외부통신·환경분기·데드코드·DB스키마)를 인덱스 JSON에서 기계 생성(zero-LLM) |
| `agents/lib/pattern_tally.py` | pattern-extractor의 `05_patterns_extracted.md` 집계 표(샘플수·신뢰도·안티패턴 수)를 개별 패턴 파일에서 기계 취합(zero-LLM) |
| `agents/lib/validator_checks.py` | validator 체크 1,2,3,4,6,7,8,9(파일존재·트리거품질·경로교차·보안·인덱스무결성·이력)를 기계 실행(zero-LLM), 체크 5·10만 validator(LLM)에 남김 |
| `agents/lib/qa_boundary6.py` | qa Boundary 6(워크플로우 스킬 ↔ 인덱스 의존성)을 기계 실행(zero-LLM) |

> 본 저장소 내의 `agents/`·`skills/` 경로는 *플러그인 소스*이며, 설치된 대상 프로젝트에서 출력되는 결과물은 여전히 대상 프로젝트의 `.claude/skills/...`·`.claude/agents/...`에 기록된다. 에이전트/스킬 본문 내부의 `.claude/...` 경로는 *대상 프로젝트* 경로를 의미한다.

## 자동 워크플로우

| 상황 | 트리거 스킬 |
|------|---------|
| 하네스 초기화 전 명세 명확화 | `spec-gate` |
| 하네스 초기화 / 재초기화 | `harness-init` |
| 하네스 전체 제거 | `harness-clean` |
| 변경 영향도 분석 | `analyze-impact` |
| 안전한 변경 진행 | `safe-modify` |
| 컨벤션 따라 신규 기능 생성 | `scaffold-feature` |
| 게이트 생략 알아서 작업 ("알아서 해줘", "그냥 해줘") | `vibe` |
| 스택 마이그레이션 계획 | `plan-migration` |
| SQL 리뷰 | `review-sql` |
| 레거시 코드 해석 | `legacy-decoder` 직접 호출 |
| 문서 동기화 | `doc-syncer` 직접 호출 |
| 기능·API 처리 흐름 추적 | `trace-logic` |
| 기능·키워드로 코드 위치 탐색 | `find-feature` |
| harness 산출물 → wiki 생성 | `generate-wiki` |
| 백엔드·프론트엔드 별도 저장소 연동 | `pair-init` |
| 전체 스택 기능 동시 생성 | `cross-repo-scaffold` |
| 기존 기능 개선/수정 양쪽 저장소 동시 반영 | `cross-repo-modify` |
| API 드리프트 감지 | `pair-init` 재실행 |
| 프론트엔드 서비스 스텁만 생성 | `api-bridge` 직접 호출 |
| 생성된 wiki를 중앙 허브(wiki-hub)에 발행 | `publish-wiki` |
| 여러 시스템 wiki 통합 열람·검색·버전관리 | `wiki-hub` |

## 에이전트 수정

에이전트 개선은 `agents/[name].md` 파일을 직접 수정한다.  
변경사항은 아래 변경 이력 테이블에 기록한다 (revfactory/harness Phase 5-4 템플릿).

새 에이전트 추가:
1. `agents/[name].md` 작성 (frontmatter + 본문)
2. 호출하는 오케스트레이터 스킬(`skills/<name>/SKILL.md`)에 등록
3. 변경 이력 테이블에 기록

새 스킬 추가:
1. `skills/[name]/SKILL.md` 폴더 + 파일 생성 (frontmatter 필수)
2. 변경 이력 테이블에 기록

## 변경 이력

| 날짜 | 변경 내용 | 대상 | 사유 |
|------|----------|------|------|
| 2026-06-02 | harness-new 기반 확장 — P0(impact-analyzer, change-safety) + P1(pattern-extractor, migration-planner) + P2(test-generator, sql-reviewer, legacy-decoder, doc-syncer) + 워크플로우 스킬 5종(analyze-impact, safe-modify, scaffold-feature, plan-migration, review-sql) + analyzer 심층 분석(의존성 그래프, 데이터 흐름, 트랜잭션, 외부 통신, 환경 분기, 데드 코드) + 인덱스 레이어(_workspace/index/*.json) | 전체 | ITO/SI 조직의 수정/개발/마이그레이션 작업까지 끊김 없이 지원하기 위함 |
| 2026-06-02 | Claude Code 플러그인 표준 레이아웃으로 재구성 — `.claude/agents/`·`.claude/skills/` 중복본 제거, 루트 `agents/`·`skills/<name>/SKILL.md` 단일 source-of-truth로 정리, `.claude-plugin/marketplace.json`·`plugin.json` 추가 | 저장소 구조 | `/plugin marketplace add neoruler001/ax-std-harness`로 설치 가능하도록 |
| 2026-06-02 | Vue.js 스택 지원 추가 — Vue 2/3, Nuxt 2/3, Pinia/Vuex, Vue Router, Vite, Vue CLI 탐지 + QA Boundary + 마이그레이션 매핑 (Vue 2→3, Vuex→Pinia, Nuxt 2→3, Vue CLI→Vite) | analyzer / qa / docs/stack-matrix / README | 프런트엔드 스택 커버리지 확장 |
| 2026-06-03 | 로직 탐색 에이전트 2종 추가 — `logic-tracer`(진입점→DB 처리 흐름 추적) + `feature-finder`(기능명·키워드 코드 위치 탐색) + 트리거 스킬 2종(`trace-logic`, `find-feature`) | agents / skills / CLAUDE.md | 특정 로직·기능 위치 탐색 요구 대응 |
| 2026-06-03 | 에이전트 모델 최적화 — opus→sonnet 8종(change-safety, pattern-extractor, validator, qa, test-generator, sql-reviewer, doc-syncer, logic-tracer). opus 유지 5종(analyzer, writer, legacy-decoder, impact-analyzer, migration-planner) | agents/ | 패턴 기반 처리 작업에 opus 불필요, 비용 절감 |
| 2026-06-03 | harness-init 3-Tier 적응 실행 — 복잡도 점수(파일수+DB/ORM+레거시+멀티모듈+외부시스템) 기반 Lite/Standard/Full 자동 분기. Lite: analyzer lite(sonnet)+writer(sonnet)+validator, QA·pattern 스킵. Standard: analyzer init(sonnet)+선택적 Phase B+writer(sonnet)+pattern+validator, QA 스킵. Full: 기존 파이프라인. 사용자 override 키워드 지원. | skills/harness-init / agents/analyzer | 프로젝트 규모 무관 전체 파이프라인 실행으로 인한 토큰·시간 낭비 해소 |
| 2026-06-04 | harness-clean 스킬 추가 — CLAUDE.md·.claude/skills/·agents/·patterns/·_workspace/ 안전 제거 + 플러그인 언인스톨 안내 | skills/harness-clean / CLAUDE.md | 마음에 안 들면 롤백할 수 있어야 표준으로 쓸 수 있음 |
| 2026-06-04 | Ouroboros 명세 게이트 + Karpathy eval 루프 접목 — spec-clarifier(sonnet, Phase -1: 소크라테스 인터뷰·모호성점수·GO신호) + harness-evaluator(sonnet, Phase 4: 4차원 품질평가·PASS/PARTIAL/RETRY·fix_targets 기반 타겟 재생성·1회 루프) + spec-gate 스킬 + harness-init Phase -1/-4 추가 + analyzer에 spec_context 전달 | agents/spec-clarifier, agents/harness-evaluator, skills/spec-gate, skills/harness-init, CLAUDE.md | 명세 모호성 제거(Ouroboros)·자기 개선 루프(Karpathy)로 harness 생성 품질 향상 |
| 2026-06-24 | wiki 생성 기능 추가 — `wiki-builder`(sonnet, harness 산출물→wiki 페이지·call_graph.json→vis-network 인터랙티브 HTML 변환·callgraph.html 템플릿 계승) + `generate-wiki` 오케스트레이터 스킬(기존 wiki 백업·페이지 범위 선택·call-graph.html 전용 페이지) + harness-init Phase 3.5(완료 후 wiki 생성 제안) | agents/wiki-builder, skills/generate-wiki, skills/harness-init, CLAUDE.md | harness 완료 후 탐색 가능한 wiki 문서 자동 생성 요구 대응 |
| 2026-06-30 | call-graph.html 디자인 전면 개편 — 우측 고정 사이드바(통계·범례·노드 상세), 타입 기반 필터 토글(opacity active/inactive), 헤더 실시간 검색, 7가지 시각 타입(view·endpoint·function·dao·external·db_table·util)·노드 모양(ellipse·box·hexagon·diamond·database·dot), COLORS+nodeTypeMap+META+mkNode() 구조로 교체. 기존 group 기반 필터·info-panel slide 방식 제거 | agents/wiki-builder, skills/generate-wiki | mfs-test 실사례 기반 디자인 표준화 |
| 2026-06-29 | Type B(별도 저장소) 크로스 리포 지원 추가 — `api-bridge`(sonnet, API 계약 extract/validate/generate-stub/check-impact 4-mode) + `pair-init` 스킬(두 레포 연동·pair_config.md 생성·API 드리프트 검증) + `cross-repo-scaffold` 스킬(전체 스택 기능 동시 스캐폴딩) + analyzer Step 0.5(pair_config 감지) + Step 15.5(API 계약 추출) + writer 파트너 섹션 + cross-repo-scaffold.md 조건부 생성 + impact-analyzer Step 8.5(파트너 영향) + harness-init Phase 3.5(pair-init 제안, 기존 wiki 제안은 Phase 3.6으로 이동) + scaffold-feature pair_config 인식 | agents/api-bridge, skills/pair-init, skills/cross-repo-scaffold, agents/analyzer, agents/writer, agents/impact-analyzer, skills/harness-init, skills/scaffold-feature, CLAUDE.md | 백엔드·프론트엔드 별도 저장소 구조에서 API 계약 기반 연계 개발·바이브 코딩 지원 |
| 2026-06-30 | harness-init Phase -2 추가 — harness-init 시작 시 프로젝트 구조(모노레포/멀티레포/단일 스택) 사용자 확인 질문. 멀티레포 선택 시 파트너 경로·역할·API URL 선수집 → Phase 3.5에서 pair-init 자동 실행(확인 생략). Phase 3.5를 분기 처리(모노레포/단일 스택은 스킵, 멀티레포+파트너 정보 있으면 자동 실행, Phase -2 스킵 시 기존 질문 방식 유지) | skills/harness-init, CLAUDE.md | harness-init 시작 시 구조 파악으로 pair-init 연동을 자연스러운 흐름으로 통합 |
| 2026-07-14 | writer 출력 정적 스킬 5종(analyze-impact/safe-modify/scaffold-feature/plan-migration/review-sql) 프로그램식 배포로 전환 — `agents/lib/skills/*.template.md`(정적 원본, 변수 없음) + `agents/lib/skills_builder.py`(harness-init Phase 2-2.3, `_workspace/02_writer_files.md`의 생성 결정 파싱 후 무-LLM 복사). writer는 이 5종 대신 plan-migration/review-sql 생성 여부 판단만 수행. CLAUDE.md 상단에 부분 실행·incremental 유도 배너 추가. impact-analyzer 모델은 opus 유지(2026-06-03 결정 재확인) | agents/lib/skills_builder.py, agents/lib/skills/*.template.md, agents/writer, skills/harness-init, CLAUDE.md | writer가 프로젝트별 변수 없는 고정 텍스트를 매번 LLM으로 재작성하던 출력 토큰 낭비 제거. 이전 시도(하이브리드 빌더)가 writer.md 미동기화·조건부 판단 로직 누락으로 5종 중 2종만 배포되고 CLAUDE.md/domain-expert/patterns 등 그룹 A 산출물이 누락되는 회귀를 유발해 재구현 |
| 2026-07-14 | 멀티레포 파트너 자동 하네스 생성 + 바이브코딩 크로스 리포 수정 지원 — (1) `pair-init` Phase 1: 파트너 하네스 없을 때 3지선다("자동 생성"/"없이 진행"/"중단") 추가, "자동 생성" 선택 시 subagent로 파트너 루트에서 harness-init 자체를 대신 실행. (2) `harness-init` Phase 3.5: harness-init이 주도하는 흐름(Phase -2에서 멀티레포 확인됨)에서는 위 3지선다 재질문 없이 자동 생성 적용 + 그 Agent 호출을 Phase 4(harness-evaluator) 호출과 같은 메시지에서 병렬 실행하도록 명시. (3) `harness-init` Phase 3.6: 파트너 하네스가 있으면 "통합 wiki 생성" 3지선다로 분기(단독 wiki를 선택해도 pair_config.md 기반 자동 병합이라 통합 그래프가 나온다는 점 안내) — 프론트엔드에서 실행해도 backend 쪽 call_graph.json이 자동 병합됨을 명시. (4) 신규 스킬 `cross-repo-modify` 추가 — 한쪽에서 지시한 기존 기능 개선을, `impact-analyzer`(시작 측) → `api-bridge check-impact`(파트너 영향 확인) → 사용자 확인 게이트 → 양쪽 변경 적용 → 양쪽 `change-safety` + `api-bridge validate`(드리프트 재검증) 순으로 안전하게 양쪽 저장소에 반영. 파트너 저장소는 별도 git/배포 프로세스라는 전제 하에, 파트너 파일 수정 전 반드시 확인받고 커밋은 절대 자동 실행하지 않음. (5) `wiki_generator.py`의 `merge_partner_call_graph()`가 병합 결과(merged 여부·파트너 노드 수·추론된 크로스 엣지 수·스킵 사유)를 `07_wiki_build.md`에 명시적으로 기록하도록 확장 — 이전에는 콘솔 로그에만 남고 보고서엔 안 남아 사용자가 통합 여부를 확인할 방법이 없었음. `generate-wiki` Phase 3 보고에도 반영. 프론트엔드 루트 기준 병합 시나리오(파트너 노드 2개 병합, 크로스 엣지 1개 정확히 추론)로 재검증 완료 | skills/pair-init, skills/harness-init, skills/cross-repo-modify, agents/lib/wiki_generator.py, skills/generate-wiki, CLAUDE.md | 백엔드·프론트엔드가 완전히 분리된 저장소/배포 구조에서 한쪽 harness-init만으로 양쪽 하네스·통합 wiki가 갖춰지고, 이후 바이브 코딩 개선 지시도 양쪽에 안전하게 전파되도록 지원 |
| 2026-07-14 | 하이브리드 빌드 확장 + wiki 회귀 수정 4건 — (1) `domain-expert.md`를 writer 재작성 대신 `_workspace/01_analyzer_report.md` 그대로 복사(skills_builder.py)로 전환, 100% 중복 출력 제거. (2) CLAUDE.md 하이브리드화 — `agents/lib/claude_md.template.md`(고정 워크플로우 표·변경이력 헤더·파트너 섹션 서식) + writer는 `_workspace/claude_md_fields.json`에 서술형 필드 7개만 출력, skills_builder.py가 조립(파트너 섹션은 pair_config.md 값을 그대로 옮기는 것뿐이라 완전 무-LLM). (3) `wiki_generator.py` 크로스 리포(pair-init) 그래프 병합 미구현 상태(주석만 있던 TODO)를 실제 구현 — 파트너 call_graph.json 노드/엣지 ID 프리픽스 병합 + api_contract.json 기반 메서드/경로 정규화 매칭으로 프론트-백엔드 크로스 엣지 자동 추론(전부 결정론적 문자열 매칭, LLM 미개입). wiki-builder.md에 pair_config 읽기 순서·통합 서술 지시 복원. (4) `wiki_generator.py`가 템플릿·vis-network 라이브러리를 project_root/agents/lib에서 찾던 버그 수정(스크립트 자신의 디렉터리 기준으로 변경) — 실제 플러그인 설치 환경(템플릿이 대상 프로젝트가 아닌 플러그인 저장소에 있음)에서는 Home.md가 항상 raw 기본값으로 폴백하고 architecture.md/call-graph.html이 아예 생성되지 않던 잠재 회귀였음. (5) wiki-builder.md의 `workflows` 필드가 예시 1개만 보고 전체 스킬을 나열 안 할 위험 있어 "설치된 스킬 전부" 명시. 전부 더미 데이터로 동작 검증 완료 | agents/lib/skills_builder.py, agents/lib/claude_md.template.md, agents/lib/wiki_generator.py, agents/wiki-builder, agents/writer, skills/harness-init | harness-init(LLM 위키)과 generate-wiki(시스템 위키) 양쪽의 토큰 절감을 이어가되, "절감하면 품질 떨어지는 거 아니냐"는 지적에 따라 실제 동작 검증 없이 존재하던 회귀·잠재 버그를 먼저 찾아 고침 |
| 2026-07-14 | 시스템 wiki MSSQL DB 저장 지원 추가 — `agents/lib/wiki_db.py`(pymssql, 프로젝트 루트 `.env`의 MSSQL_HOST/PORT/USER/PASSWORD/DATABASE로 접속, `harness_wiki_pages` 테이블에 페이지 upsert/조회, 폴더↔DB 완전 동기화) + `agents/lib/wiki_db_server.py`(stdlib http.server 기반 로컬 뷰어 — markdown은 이스케이프 후 렌더링, call-graph.html은 그대로 서빙, vis-network 정적 파일은 플러그인 lib에서 직접 서빙, 외부 CDN 의존 없음) + `generate-wiki` Phase 1.5(저장 위치 폴더/DB 선택 질문, `wiki_generator.py --storage folder\|db` 연동) + 신규 스킬 `sync-wiki`(폴더→DB, DB→폴더 양방향 수동 변환) + `agents/lib/requirements.txt`(pymssql) + `.env.example` 추가 + `.gitignore`에 `.env` 추가(실 접속정보 커밋 방지). 실제 MSSQL 서버(사용자 제공 계정, CREATE DATABASE 권한 없어 계정 기본 DB 사용으로 조정)에 연결해 테이블 생성·폴더→DB→폴더 왕복·DB 뷰어 HTTP 응답까지 전부 실제 실행으로 검증(테스트 데이터는 완료 후 삭제, 스키마는 유지) | agents/lib/wiki_db.py, agents/lib/wiki_db_server.py, agents/lib/wiki_generator.py, agents/lib/requirements.txt, skills/generate-wiki, skills/sync-wiki, .env.example, .gitignore, CLAUDE.md | 시스템 wiki를 파일이 아닌 DB에 보관해 여러 사용자가 공유 조회할 수 있게 하고, 폴더/DB 두 형태를 언제든 상호 변환 가능하게 하기 위함 |
| 2026-07-14 | wiki DB 저장의 다중 시스템 구분을 `WIKI_SYSTEM_KEY` 명시 입력 방식으로 강화 — 기존엔 프로젝트 폴더 basename으로만 구분해 서로 다른 시스템의 폴더명이 같으면(예: 둘 다 `backend/`) 데이터가 섞이는 문제 발견. 스키마 변경 없이(`harness_wiki_pages` 그대로, 당시 0행이라 마이그레이션 리스크 없었음 확인) `project_name` 값의 결정 로직만 `--system-key`(1회성 override) > `.env`의 `WIKI_SYSTEM_KEY` > 폴더명(경고 출력) 우선순위로 변경. `resolve_system_key()`/`ensure_system_key_in_env()`(기존 키 있으면 덮어쓰지 않음) 추가, `save_folder_to_db`/`load_db_to_folder`/`list_pages`/`get_page` 전부 `system_key` 파라미터 지원, `--list-systems` CLI(DB에 저장된 전체 시스템 + 페이지 수 + 최근 갱신 시각 조회) 추가. `generate-wiki`·`sync-wiki`는 DB 저장/DB→폴더 시 시스템 키를 묻고 `.env`에 저장(재실행 시 재사용). `wiki_db_server.py`는 `--system-key` + `?key=` 쿼리 파라미터로 시스템 전환, 인덱스 페이지에 "다른 시스템" 목록 표시. 폴더명이 같은 더미 시스템 2개(`SYS-A-BACKEND`, `SYS-B-BACKEND`)를 실제 DB에 저장해 데이터가 섞이지 않고 분리됨을 `--list-systems`·페이지 조회·서버 전환 링크로 각각 실제 실행 검증(검증 후 테스트 행 삭제, 스키마는 유지) | agents/lib/wiki_db.py, agents/lib/wiki_db_server.py, skills/generate-wiki, skills/sync-wiki, CLAUDE.md | 여러 시스템(ITO/SI에서 흔한 backend/frontend 등 동일 폴더명 반복 구조)의 wiki를 하나의 DB에 안전하게 공존시키기 위함 |
| 2026-07-14 | 폴더 모드 wiki의 브라우저 열람 기능이 실제로는 존재하지 않던 문제 발견 및 수정 — 기존 안내문("브라우저로 wiki/index.html 직접 열기")과 달리 `wiki_generator.py`는 `index.html`을 아예 생성하지 않았고(재확인용 grep으로 검증), 설령 있었어도 docsify류 클라이언트 fetch 방식은 `file://`로 열면 브라우저 CORS 정책에 막혀 대부분 동작하지 않았을 것(로컬 서버 필요). 실제로 폴더 모드에서 서버 없이 정상 동작하는 건 데이터가 인라인된 `call-graph.html` 뿐이었고 `Home.md`/`architecture.md`/`workflows.md`는 서식 없는 원본 텍스트로만 보였음. `wiki_db_server.py`의 이스케이프+`<pre>` 렌더 로직을 `agents/lib/wiki_render.py`(신규, 공용 모듈)로 추출해 `wiki_db_server.py`(리팩터, 기존 curl 회귀 테스트로 동일 동작 재확인)와 `wiki_generator.py`(신규: 각 `.md` 페이지 저장 직후 `wiki/_html/<name>.html` 정적 렌더 생성 + `wiki/index.html` 랜딩 페이지 생성) 양쪽에서 공유. `wiki_db.py`의 `load_db_to_folder`(DB→폴더)에도 동일하게 정적 렌더 생성을 추가해 폴더 모드가 어느 경로로 만들어지든(생성 직후/DB에서 복원) 항상 서버 없이 더블클릭으로 열람 가능하게 함. `_iter_wiki_files`는 `_html/`·`index.html`을 실제 콘텐츠가 아닌 보기 전용 파일로 보고 DB 동기화 대상에서 제외(안 그러면 Home.md/Home.html이 DB에 중복 저장됨). 실제 더미 프로젝트로 폴더 생성→파일 트리 확인→폴더→DB(4페이지, `_html`/`index.html` 제외 확인)→DB→폴더 복원(정적 렌더 재생성 확인)까지 전부 실행 검증(테스트 데이터 삭제, 스키마 유지) | agents/lib/wiki_render.py, agents/lib/wiki_db_server.py, agents/lib/wiki_generator.py, agents/lib/wiki_db.py, skills/generate-wiki, skills/sync-wiki, CLAUDE.md | 사용자가 "DB/폴더 두 경우 모두 브라우저로 볼 수 있는 것까지 적용됐는지" 재확인 요청 — 실제로 다시 읽어보니 폴더 모드 쪽 안내 문구가 실제 코드와 어긋나 있었음(과거 세션에 실제 실행 검증 없이 넘어간 부분) |
| 2026-07-15 | 폴더 모드 wiki의 `index.html`을 정적 랜딩 페이지에서 Docsify 4 기반으로 교체 — `render_index()`(`agents/lib/wiki_render.py`)가 Docsify 부트스트랩 HTML(외부 CDN: docsify·docsify-themeable·pretendard·prism·검색/페이지네이션/복사 플러그인)을 생성하도록 변경, `wiki_generator.py`에 `generate_sidebar_md()`/`generate_navbar_md()`/`generate_serve_bat()` 추가로 `wiki/_sidebar.md`·`wiki/_navbar.md`·`wiki/serve.bat`(`python -m http.server 3501`)을 함께 생성. 신규 `agents/lib/docsify_convert.py`(레거시 wiki 폴더를 Docsify로 수동 재변환하는 독립 CLI, wiki_generator.py 미실행). 이 변경은 다른 시스템에서 harness-init을 실행하며 플러그인 캐시(`~/.claude/plugins/cache/ax-std-harness/ax-std-harness/0.4.0`)에 직접 적용됐던 수정을 diff로 발견해 본 저장소(원본)에 역이식한 것 — `wiki_render.py` 헤더 주석(`render_markdown_page`는 CDN-프리·`file://` 호환 유지, `render_index`만 예외)과 `generate-wiki`/`sync-wiki` 스킬의 "더블클릭 열람, 서버·CDN 불필요" 안내 문구를 Docsify 요건(serve.bat 실행 필요, 인터넷 CDN 필요)에 맞게 갱신, `.gitignore`에 `__pycache__/` 추가. `wiki/_html/*.html` 개별 페이지·`wiki/call-graph.html`은 기존대로 서버 없이 열람 가능 상태 유지 | agents/lib/wiki_render.py, agents/lib/wiki_generator.py, agents/lib/docsify_convert.py, skills/generate-wiki, skills/sync-wiki, .gitignore, CLAUDE.md | 다른 환경(플러그인 캐시 직접 수정)에서 먼저 이뤄진 wiki UI 개선(네비게이션·검색·문법 강조 있는 Docsify)을 원본 저장소에 반영해 단일 소스로 유지하기 위함 |
| 2026-07-15 | DB 모드 wiki 뷰어를 Docsify와 맞물리도록 수정 — `agents/lib/wiki_db_server.py`의 markdown 페이지 응답을 `text/html`(서버 사전 렌더) 대신 `text/plain` raw markdown으로 변경(Docsify JS가 클라이언트에서 fetch·렌더). 이 변경만 플러그인 캐시에서 이식했을 경우 `_render_index()`가 여전히 구 시그니처(`heading`/`entries`/`extra_html`)로 `wiki_render.render_index()`를 호출하고 있어(2026-07-15 앞선 Docsify 이식에서 이미 그 인자들이 무시되도록 바뀜) DB 모드에서 페이지 목록·"다른 시스템" 전환 UI가 통째로 사라지는 상태였음 — `_render_index()`를 title만 넘기도록 단순화하고, `/_sidebar.md`·`/_navbar.md` 라우트를 신규 추가해 `docsify_convert.build_sidebar/build_navbar`로 DB에 저장된 페이지 목록 기반 사이드바·네비바를 동적 생성, "다른 시스템" 섹션은 사이드바 하단에 `:ignore` 링크로 추가해 복구. 미사용 상태였던 `_render_markdown_page()`는 삭제 | agents/lib/wiki_db_server.py, CLAUDE.md | 사용자가 다른 시스템의 플러그인 캐시에서 진행한 추가 수정을 재요청해 diff로 발견 — 이식만 하면 DB 뷰어가 폴더 모드보다 기능이 퇴화한 상태로 남을 것이 확인되어 함께 수정 |
| 2026-07-15 | `wiki-builder` 에이전트(LLM) 제거 + zero-LLM 크로스 리포 markdown 통합 — 사용자가 "wiki가 `_workspace`/`.claude` 산출물을 그대로 보여주면 되는데 별도 폴더에 LLM으로 다시 요약·재작성하는 건 토큰 낭비 아니냐"고 지적. 실제로 `wiki/` 8개 페이지 중 call-graph.html/index.html/_sidebar.md/_navbar.md/serve.bat/_html 렌더 사본을 뺀 나머지(Home/architecture/workflows/api-endpoints/database/patterns/external-systems/issues)는 이미 존재하는 `CLAUDE.md`·`_workspace/01_analyzer_report.md`·`.claude/skills·patterns/*.md`·`_workspace/index/*.json`을 `wiki-builder`(sonnet, 요청당 약 25~55K 토큰)가 다시 읽어 `_workspace/07_wiki_summary.json`으로 재서술한 뒤 템플릿 치환한 것에 불과했음(확인 후 삭제). 신규 `agents/lib/wiki_content.py`(LLM 미사용 순수 함수 모음)가 이를 대체 — Home←CLAUDE.md 그대로, architecture←01_analyzer_report.md 그대로, workflows/patterns←.claude/skills·patterns/*.md를 재작성 없이 목차+연결, api-endpoints/database/external-systems←api_contract.json/schema.json+sql_usage.json/external_io.json을 Python으로 표 변환(call-graph.html이 이미 하던 것과 동일 패턴), issues←03_validator_report.md+04_qa_report.md+dead_code.json 연결. `agents/wiki-builder.md`·`agents/lib/Home.template.md`·`agents/lib/architecture.template.md` 삭제, `wiki_generator.py`의 `--summary` 인자 제거(`--root`만으로 직접 읽음), `api_exists` 판정을 잘못된 소스(symbols.json)에서 올바른 소스(api_contract.json)로 교정, 자체 구현이던 사이드바/네비바 생성을 `docsify_convert.build_sidebar/build_navbar`(+ 신규 `serve_bat_content()`) 재사용으로 정리해 두 파일 간 중복 제거. **추가로** 페어 연동(`pair_config.md`, 백엔드·프론트엔드 별도 저장소) 시 지금까지 call-graph.html만 파트너 그래프를 병합하고 나머지 markdown 페이지는 자기 저장소 것만 보여주던 것을, architecture/api-endpoints/database/external-systems 4개 페이지도 파트너의 동일 산출물을 같은 zero-LLM 방식으로 읽어 "## 파트너 ({partner_type})" 섹션으로 병합하도록 확장(어느 쪽에서 `generate-wiki`를 실행해도 통합됨, 기존 call-graph 병합과 동일 원칙) — 사람이 wiki 하나로 백엔드+프론트엔드 전체 시스템을 파악하고, 바이브 코딩 시 한쪽 요청만 와도 LLM이 양쪽 분석 내용을 참고할 수 있게 하기 위함. workflows/patterns/issues는 저장소별 실행 환경·품질 이슈라 병합 대상에서 제외(설계 결정, 근거는 계획 문서 참조). `skills/cross-repo-modify`·`skills/cross-repo-scaffold`에는 통합 architecture.md를 오리엔테이션 참고용으로만 쓰고 실제 영향 분석·드리프트 검증은 기존대로 `impact-analyzer`/`api-bridge` 라이브 재분석으로 수행한다는 원칙을 한 줄 추가(wiki는 스냅샷이라 최신성 미보장) | agents/lib/wiki_content.py(신규), agents/lib/wiki_generator.py, agents/lib/docsify_convert.py, agents/wiki-builder.md(삭제), agents/lib/Home.template.md(삭제), agents/lib/architecture.template.md(삭제), skills/generate-wiki/SKILL.md, skills/harness-init/SKILL.md, skills/cross-repo-modify/SKILL.md, skills/cross-repo-scaffold/SKILL.md, CLAUDE.md | wiki-builder의 LLM 재서술 비용이 대부분 이미 있는 문서의 중복 표현이었음을 확인했고, 크로스 리포 구조에서 사람·LLM 모두 시스템 전체를 한 곳에서 파악할 수 있어야 바이브 코딩 요청을 정확히 처리할 수 있기 때문 |
| 2026-07-15 | harness-init 파이프라인(analyzer/pattern-extractor/writer/validator/qa) 중 "이미 구조화된 JSON을 LLM이 다시 프로즈로 옮겨 적는" 부분만 골라 zero-LLM 기계화 (판단이 필요한 부분은 템플릿 고정 없이 그대로 LLM 유지 — 대형/소형 시스템마다 분석 내용이 달라지는 부분을 고정하면 누락 위험이 생기기 때문). (1) `agents/lib/analyzer_index_summary.py`(신규) — analyzer 리포트 Section B(의존성그래프·트랜잭션·외부통신·환경분기·데드코드)/D(DB스키마)를 `_workspace/index/*.json`에서 카운트·표로 생성, analyzer.md는 Section A와 대응 인덱스가 없는 "비동기/스케줄/이벤트"·"인증/인가 경로"·"탐지 신뢰도"·"보완 권장"만 직접 작성. (2) `agents/lib/pattern_tally.py`(신규) — `05_patterns_extracted.md`의 집계 표(샘플수·신뢰도·안티패턴 발견 수)를 각 `.claude/patterns/*.md`에서 정규식으로 취합, pattern-extractor는 패턴 본문과 "## 권고"만 직접 작성. (3) `agents/lib/skills_builder.py` 확장 — patterns/ 스켈레톤(고정 헤더, client_pattern.md는 "LegacyStaticJS" 탐지 시 자동 추가, 이미 채워진 파일은 안 덮어씀) + `02_writer_files.md`(고정 서식) 조립 추가. writer는 이제 이 두 산출물 대신 `_workspace/writer_decisions.json`(조건부 스킬 생성 여부+사유, pattern_files 목록, 탐지 스택, 적용 결정 사유)만 출력 — `decision_for()`도 `02_writer_files.md` 정규식 파싱에서 JSON 직접 로드로 계약 변경(writer.md와 한 번에 반영). (4) `agents/lib/validator_checks.py`(신규) — validator 10개 체크 중 8개(1,2,3,4,6,7,8,9: 파일존재·트리거품질·경로교차·보안regex·인덱스무결성·harness-init.md보존·변경이력)를 기계 실행해 `_workspace/validator_mechanical.json`(기존 점수 산식 그대로, `report_fragments`에 리포트 섹션 텍스트 포함) 생성, validator(LLM)는 체크 5(레이어 커버리지)와 체크 10 중 스크립트가 "판정 불가"로 남긴 항목만 담당. (5) `agents/lib/qa_boundary6.py`(신규) — qa Boundary 6(워크플로우 스킬 ↔ 인덱스 의존성)을 기계 실행, Boundary 1~5·7(실제 소스 읽고 의미 비교 필요)은 그대로 qa(LLM) 유지. `skills/harness-init/SKILL.md`의 Phase 2-4/2-5에 두 스크립트 실행 단계를 validator/qa Agent 호출 직전에 추가(실패 시 WARN 후 기존 방식 폴백). 더미 backend 프로젝트로 5개 스크립트를 실제 파이프라인 순서(analyzer_index_summary→skills_builder→pattern_tally→validator_checks→qa_boundary6)대로 전부 실행 검증 — 이 과정에서 check 3(스킬 트리거 품질)이 정적 배포 스킬 5종·harness-init.md(둘 다 프로젝트별로 새로 판단할 트리거가 없는 고정 파일)까지 잘못 채점해 매 프로젝트마다 불필요한 FAIL 4건이 나오는 실제 버그를 발견해 수정(`STATIC_OR_PREEXISTING_SKILLS` 제외 목록 추가) | agents/lib/analyzer_index_summary.py(신규), agents/lib/pattern_tally.py(신규), agents/lib/validator_checks.py(신규), agents/lib/qa_boundary6.py(신규), agents/lib/skills_builder.py, agents/analyzer.md, agents/pattern-extractor.md, agents/writer.md, agents/validator.md, agents/qa.md, skills/harness-init/SKILL.md, CLAUDE.md | 사용자가 "토큰 절감이 목적이 아니라, 불필요한 재서술 작업만 프로그램화하고 실제 판단이 필요한 부분은 절대 템플릿으로 고정하지 말라"고 명시 — wiki-builder 제거와 동일한 원칙을 파이프라인 나머지 단계에도 적용해달라는 요청 |
| 2026-07-15 | 전체 agents/skills 재검토(fork 2개 병렬 감사) 결과 2건 수정. (1) opus/sonnet 불일치 회귀 4건 수정 — `skills/review-sql`(→sql-reviewer), `skills/safe-modify`(→change-safety), `skills/scaffold-feature`(→test-generator), `skills/trace-logic`(→logic-tracer)의 `Agent()` 호출이 대상 에이전트 frontmatter(`model: sonnet`, 2026-06-03 결정)와 다르게 `model="opus"`로 하드코딩되어 있어 매 호출 opus로 실행되던 것을 `model="sonnet"`으로 수정(analyze-impact/plan-migration은 impact-analyzer/migration-planner가 의도적으로 opus 유지 대상이라 그대로 둠, 확인 완료). (2) 스킬 4개(`review-sql`/`safe-modify`/`scaffold-feature`/`harness-init`) 끝부분 "시나리오 예시" 섹션 삭제 — 앞선 Phase별 지시를 이야기 형식으로 재진술만 할 뿐 새 판단 로직이 없어 매 스킬 호출 시 순수 재서술 토큰만 소비하던 부분 제거 | skills/review-sql/SKILL.md, skills/safe-modify/SKILL.md, skills/scaffold-feature/SKILL.md, skills/trace-logic/SKILL.md, skills/harness-init/SKILL.md, CLAUDE.md | 사용자가 "harness 생성하는 모든 agent/skill 전반 검증해서 성능 유지하며 토큰 절약 방안 검토" 요청 — fork 2개로 agents/*.md·skills/*/SKILL.md 각각 감사해 opus 하드코딩 회귀와 순수 재서술 섹션을 발견 |
| 2026-07-16 | Karpathy 가이드라인(CLAUDE (1).md) 10개 규칙을 CLAUDE.md에 "보편 에이전트 행동 원칙" 섹션으로 통합 — Rule 1(코딩 전 사고)·Rule 2(단순성)·Rule 3(외과적 변경)·Rule 5(한국어 콜론 금지)·Rule 6(파일 헤더)·Rule 9(의미 커밋)·Rule 10(에러 읽기)를 직접 명시. Rule 4/7/8은 이미 `_workspace/` 파이프라인으로 구현된 사실을 참조 형태로 명기. `skills/safe-modify/SKILL.md`에 "변경 범위 원칙(Rule: 외과적 변경)" 섹션 추가(모든 Phase에 최우선 적용). `agents/lib/*.py` 8개 파일(analyzer_index_summary·pattern_tally·qa_boundary6·skills_builder·validator_checks·wiki_db·wiki_db_server·wiki_generator)에 한국어 헤더 주석 소급 적용 | CLAUDE.md, skills/safe-modify/SKILL.md, agents/lib/analyzer_index_summary.py, agents/lib/pattern_tally.py, agents/lib/qa_boundary6.py, agents/lib/skills_builder.py, agents/lib/validator_checks.py, agents/lib/wiki_db.py, agents/lib/wiki_db_server.py, agents/lib/wiki_generator.py | 사용자가 CLAUDE (1).md(Karpathy 가이드라인)의 내용이 잘 적용될 수 있도록 검토 및 적용 요청 |
| 2026-07-22 | 플러그인 버전 0.4.0 → 0.5.0 (`.claude-plugin/plugin.json`). 이번 세션의 harness-init Phase -1/-2 병합, 진행상황 한글표시 이식, QA·wiki 온디맨드화, 후속 드리프트 수정 4건을 반영한 버전 표시 | .claude-plugin/plugin.json, CLAUDE.md | 로컬 디렉터리 마켓플레이스로 설치돼 있어 파일 자체가 곧 배포본이므로, 버전 번호는 실제 배포 산출물이 아니라 변경 이력 추적용 |
| 2026-07-22 | 사용자 요청으로 harness-init 전체 프로세스(직전 3건 변경분)를 점검해 발견한 문서·로직 드리프트 7건 수정. (1) **기능 버그**: `skills/pair-init/SKILL.md`의 "파트너 하네스 자동 생성" subagent 호출이 옛 변수명 `repo_structure`·존재하지 않는 "Phase -2"를 참조하고 있었고, 새로 추가된 Phase 0 Step 2.5(Tier 확인 질문)·Phase 3.6(QA·wiki 선택 메뉴)이 응답할 사용자가 없는 무인 subagent 실행에서 그대로 질문을 시도해 멈출 수 있는 상태였음 — `init_layout: 'paired-roots'`로 교정하고, 두 질문 모두 명시적으로 "묻지 않고 기본값(Full/지금 안 함)으로 진행" 지시를 추가. (2) `skills/harness-init/SKILL.md` 자체에 남아있던 stale 참조 정리 — 상단 "필수 파이프라인" 소개에서 qa 제거(온디맨드 항목으로 별도 명시), Step 3 실행모드 표의 "qa" 포함 문구 수정, Step 2.5 Tier별 실행구성 표에서 Lite/Standard만 "QA 스킵"으로 표시돼 있어 Full은 자동 실행되는 것처럼 보이던 오독 여지를 제거(QA는 세 Tier 모두 스킵, 별도 각주로 명시), Phase 3.6에 "Phase 3.5·3.6 질문은 순서대로 제시" 안내문 복원(재작성 시 누락됨). (3) `agents/spec-clarifier.md`·`skills/spec-gate/SKILL.md`의 description이 "harness-init Phase -1에서 자동 호출"이라고 명시하고 있었으나 실제로는 지난 변경에서 harness-init이 spec-clarifier 호출을 완전히 제거했음 — 두 파일 모두 "harness-init은 자동 호출하지 않음, 필요시 별도 호출"로 정정. (4) `README.md`·`docs/user-guide.md`·`docs/harness-description.md` 3개 문서에 남아있던 옛 설계(스펙 인터뷰 질문 흐름, 복잡도 점수 기반 자동 Tier 판정 mermaid 다이어그램·표, `00_spec_report.md`를 harness-init 산출물로 표기, "경계면 QA"를 Full Tier 전용으로 표기)를 실제 동작(구성확인 4택 + Tier 1회 확인질문, QA/wiki 전부 온디맨드, `00_init_scope.md`)에 맞게 재작성 | skills/pair-init/SKILL.md, skills/harness-init/SKILL.md, agents/spec-clarifier.md, skills/spec-gate/SKILL.md, README.md, docs/user-guide.md, docs/harness-description.md, CLAUDE.md | 사용자가 "수정된 전체 프로세스를 점검해서 오류 있는지, 정상 작동하는지 확인해달라"고 요청 — 직전 세 차례의 harness-init 수정(Phase -1/-2 병합, 진행상황 한글표시, QA·wiki 온디맨드화)이 SKILL.md 자체는 일관됐지만 연동 스킬(pair-init)과 사용자 문서(README/user-guide/harness-description) 쪽 반영이 누락돼 실제 동작과 문서·일부 로직이 어긋난 상태였음 |
| 2026-07-22 | AX-Harness-INTG-main처럼 QA(경계면 교차 비교)와 wiki 생성을 온디맨드 선택 작업으로 통합해 토큰 절감. 기존엔 QA가 Full Tier에서만 Phase 2 파이프라인 안에서 자동 실행됐음(Lite/Standard는 스킵, Full은 무조건 실행) — 이를 Tier 무관하게 항상 스킵하도록 바꾸고, Phase 1 Full 작업그래프에서 `T-Q` 제거(`T-E`는 `T-Q` 대신 `T-V`에 blockedBy), Phase 2의 `### 2-5. qa 호출` 섹션 삭제(agent 호출 스펙은 보존해 Phase 3.6으로 이동), `### 2-6. harness-evaluator`를 `### 2-5.`로 재번호. 기존 Phase 3.6(wiki 단독 질문)을 "Phase 3.6: 선택 작업 안내 (QA·Wiki)"로 교체 — INTG의 번호 메뉴 방식(복수 선택 가능, 무응답 시 아무것도 실행 안 함)을 그대로 적용해 QA와 wiki를 한 번에 묻고, 선택한 것만 실행. validator 신뢰도 < 50이면 QA 옵션에 "구조 검증 실패로 결과 없이 종료됨" 표시. 하위 참조 정리 — Phase 3 보고 템플릿의 "경계면 교차 비교(qa)" 섹션을 조건부로, 항상 읽던 `04_qa_report.md` 참조를 "(있으면)"으로, 에러 핸들링 표의 "qa 스킵" 문구를 "QA 옵션 실행 시 결과 없이 종료"로, Phase 4 fix_targets task-id 매핑에서 이제 안 쓰는 `qa→T-Q`를 제거(fix_target.agent는 애초에 analyzer/writer만 반환됨을 harness-evaluator.md에서 재확인). `README.md`의 에이전트 표·`agents/qa.md` description·`docs/workflows.md`의 안내 문구도 "Full만"→"온디맨드"로 갱신 | skills/harness-init/SKILL.md, README.md, agents/qa.md, docs/workflows.md, CLAUDE.md | 사용자가 AX-Harness-INTG-main의 QA·wiki 질의응답 방식(자동 실행 대신 사용자에게 묻고 선택한 것만 실행)을 이식해달라고 명시적으로 요청, 목적은 토큰 절감 — pattern-extractor·harness-evaluator 자동 루프는 이번 요청 범위 밖이라 건드리지 않음 |
| 2026-07-22 | AX-Harness-INTG-main의 진행상황 한글 표시 관례를 harness-init에 이식. 모든 `Agent()` 호출이 `subagent_type="general-purpose"`라 호스트 진행 화면에 `general-purpose`만 노출되던 문제를 보완 — `description` 필드를 `[task-id] · [실제 에이전트 이름] · 한글 목적` 형식(예: `T-A · analyzer · 프로젝트 구조·의존성·레거시 로직 분석`)으로 전부 교체(2-1 analyzer, 2-2 writer, 2-2.5 ito-guide, 2-3 pattern-extractor, 2-4 validator, 2-5 qa, 2-6 harness-evaluator, Phase 4 재생성·재평가 6곳). Phase 1에 표시 원칙 문단 추가 — `TaskCreate` 제목도 같은 형식 그대로 사용, `TaskCreate` 없는 호스트는 `_workspace/00_pipeline_status.md` 체크리스트로 폴백해도 동일 제목 유지, 보완·재검증 작업은 `T-A-RETRY`/`T-W-RETRY`/`T-V-RECHECK`/`T-E-RECHECK`처럼 원 단계 ID를 보존 | skills/harness-init/SKILL.md, CLAUDE.md | 사용자가 AX-Harness-INTG-main처럼 harness-init 진행사항이 한글로 표시돼야 한다고 지적 — 원인은 표시 문구가 아니라 모든 Agent 호출이 general-purpose로 위임되어 있어 호스트 진행 UI가 실제 단계를 구분해 보여줄 수 없었던 것이었음 |
| 2026-07-22 | 별도 프로젝트(AX-Harness-INTG-main)와 harness-init 단계별 비교 후 초기 확인 구간 2건 교체. (1) 기존 Phase -2(저장소 구조 3택: mono/multi/single) + Phase -1(spec-clarifier Ouroboros 인터뷰)을 단일 "Phase -1: 프로젝트 구성 확인"(4택: single-root/monorepo/paired-roots/selected-paths, 4번 "특정 폴더·모듈만" 신규 추가)으로 교체. `repo_structure` 변수를 `init_layout`으로 개명(mono→monorepo, multi→paired-roots, single→single-root), 확인 결과를 `_workspace/00_init_scope.md`에 영구 기록해 재실행 시 재질문 방지. spec-clarifier는 harness-init 호출부에서 제거(에이전트 파일·`spec-gate` 스킬은 그대로 유지, 독립 호출은 계속 가능). (2) Phase 0 Step 2.5를 "복잡도 점수 계산→Tier 자동결정" 방식에서 "기본 Full 고정 + override 키워드 없으면 Standard 다운그레이드 1회 확인질문"으로 교체 — 소스파일수/DB-ORM/레거시스택/멀티모듈/외부시스템 점수 계산 로직 전체 삭제(Tier 결정에 더 이상 안 쓰여 로직 자체 제거, 참고용으로도 남기지 않음). 하위 참조 정리 — analyzer 프롬프트의 `spec_context` 참조, harness-evaluator 입력의 `00_spec_report.md` 참조, 에러 핸들링 표의 "spec-clarifier 실패" 행, Phase 3 보고의 "복잡도 점수: N점" 표기, Phase 3.5 분기 표의 `repo_structure` 조건을 전부 `init_layout` 체계로 갱신 | skills/harness-init/SKILL.md, CLAUDE.md | 사용자가 두 프로젝트의 harness-init을 단계별로 비교해 장점을 흡수해달라고 요청 — Ouroboros 모호성 게이트 소실과 "질문 없는 자동 Tier 결정"이 "매번 1회 확인질문"으로 바뀌는 트레이드오프 2건을 먼저 제시해 사용자 승인 받은 뒤 진행 |
| 2026-07-23 | 전체 harness-init 파이프라인 감사(에이전트 3개 병렬 — 계약 정합성·lib 스크립트 계약·산출물 정확성 보증) 후 발견 결함 수정 5개 커밋. (1) **설치 환경 경로 결함**: 모든 `python agents/lib/*.py` 호출이 cwd 상대경로라 플러그인 설치 환경(cwd=대상 프로젝트)에서 스크립트를 못 찾던 문제 — skill/agent 7개 파일을 `$env:CLAUDE_PLUGIN_ROOT` 기준 절대경로로 교체, `--out`/`--summary` cwd 상대 인자 제거(파트너 자동 생성 흐름에서 엉뚱한 프로젝트에 읽기/쓰기 되던 문제 동시 해소), skills_builder `--summary` optional화. (2) **Lite Tier 폐지** (사용자 지시) — 인덱스 없는 하네스는 후속 스킬이 동작 안 해 실효 없고 validator check7/evaluator가 인덱스 부재를 상시 감점하는 모순 존재. "빠르게" 키워드는 Standard로 매핑, analyzer lite 모드 삭제, Standard 필수 Phase에 C(인덱스) 누락 교정. (3) **evaluator 옛 설계 채점 제거** — 정적 배포 스킬 5종을 여전히 writer 커스터마이즈 대상으로 채점하던 상시 감점 제거(배포 여부만 확인), fix_targets 복수 에이전트 행("analyzer+writer")을 단일 행 2개로 분리, Phase 4 재실행 model을 에이전트별 규칙으로 명시(writer 전 Tier sonnet). (4) **BOM 결함**: PowerShell 5.1산 JSON(BOM)에서 json.load 실패(더미 실행 재현) — 파이프라인 스크립트 5종 읽기를 utf-8-sig로 전환 | skills/harness-init, skills/generate-wiki, skills/sync-wiki, agents/analyzer.md, agents/qa.md, agents/pattern-extractor.md, agents/harness-evaluator.md, agents/lib/*.py, README, docs/ | 사용자가 "harness-init 전체 단계 점검 + LLM 산출물 정확성 점검" 요청 후 "순서대로 진행, Lite tier는 disabled 처리(기본 Full)" 지시 |
| 2026-07-23 | ito-guide.md 기계화 + 인덱스 내용 정확성 게이트 신설. (1) 고아 WIP였던 `ito_guide` 템플릿을 실제 구현으로 완성 — `skills_builder.deploy_ito_guide()`가 배포된 스킬 frontmatter(용도·트리거 regex 추출)·claude_md_fields(주의사항)·writer_decisions(스택·패턴)·pair_config(파트너 블록)만으로 조립, 실전 시나리오는 배포 스킬 조합 규칙 기반. harness-init 2-2.5의 sonnet Agent 호출(매회 ~5K 토큰) 삭제. (2) **정확성 게이트**: 기존 검증이 전부 구조(존재·파싱·카운트)만 보고 내용 검증은 온디맨드 QA에만 있어 기본 경로에 정확성 게이트가 0개였음 — validator_checks.py에 check 7b(call_graph 엣지 20개·sql_usage 10개 결정론적 샘플을 실제 소스와 대조, 일치율 <80%면 -15) 추가, analyzer.md에 인덱스 `_meta` 필수 필드(git_commit/sampled/files_scanned/files_total)·dead_code 진입점 화이트리스트(@Controller/@Scheduled/main 등)+샘플링 파생 confidence=low 강제·incremental stale 엣지 무효화 규칙 명시, analyzer_index_summary.py가 분석 커버리지(N/M 파일 %)를 기계 출력. (3) agents/lib 템플릿 7종을 `*.md.template`로 rename — `agents/` 재귀 스캔으로 가짜 에이전트 8개가 등록되던 문제 해소. (4) 잔여 드리프트 정리 — validator.md `client_side_pattern.md` 오탈자(매 실행 허위 WARN), QA 임계 40/50 → 50 통일, qa.md 존재하지 않는 `full` 모드 → `init`, pattern-extractor 스켈레톤 주체 stale 표기, 2-1 표에 incremental 행 추가. 전부 더미 프로젝트 실행으로 검증(스팟체크 불일치 검출·커버리지 라인·ito-guide 플레이스홀더 잔존 0 확인). 플러그인 버전 0.5.0 → 0.6.0 | agents/lib/skills_builder.py, agents/lib/validator_checks.py, agents/lib/analyzer_index_summary.py, agents/lib/*.md.template, agents/analyzer.md, agents/validator.md, agents/qa.md, agents/pattern-extractor.md, skills/harness-init, docs/index-spec.md, .claude-plugin/plugin.json | LLM이 만든 인덱스(call_graph 등)가 정확한지 기본 파이프라인이 전혀 검증하지 않는다는 감사 결론에 대한 저비용 기계 게이트 도입 + 재서술 기계화 원칙(wiki-builder 제거와 동일)을 ito-guide에도 적용 |
| 2026-07-23 | call-graph.html의 우측 정보 패널을 AX-Harness-INTG-main의 `build-wiki.mjs` graphHtml() 디자인에 맞춰 교체. 기존 2x2 통계 박스(노드/엣지 + 상위 타입 2개) → 단일 히어로 통계(큰 숫자 + "노드 · N 엣지" + "허브 X · 데드코드 후보 Y" 한 줄), 범례에서 타입별 "(N개)" 카운트 제거하고 2열 그리드로 재배치 + "◎ 허브(in-degree ≥ N)"·"☠ 데드 코드 후보" 안내 문구 추가, "노드 상세" 라벨을 "선택 노드"·빈 상태 문구를 "노드를 클릭하세요."로 교체. 툴바에 물리엔진 ON/OFF 토글 버튼(기존엔 항상 ON 고정, 토글 UI 없었음) + "찾기"(검색어로 노드 포커스)·"강조 초기화" 버튼 추가, 더블클릭 시 연결 노드만 강조하고 나머지는 opacity 0.15로 흐리게 하는 방식으로 변경(기존 vis-network 기본 selectNodes만 사용해 시각적 강조가 약했음). `wiki_generator.py`의 `{{EXTRA_STATS}}` 플레이스홀더를 `{{STAT_SUMMARY}}`로 교체하고 hub_count/dead_count 계산 로직 추가. 데이터 파이프라인(Python 쪽 zero-LLM 생성, 노드 타입 7종 COLORS/type_mapping)은 그대로 유지 — INTG는 별도 Node.js 기반 통합 wiki 시스템이라 그 코드 자체를 이식한 게 아니라 이 프로젝트의 사이드바 UI/문구만 동일하게 맞춘 것. 더미 프로젝트(노드 7·엣지 6·데드코드 1)로 실제 생성 실행 후 플레이스홀더 잔존 여부·통계/범례 출력값·JS 문법(node --check)까지 검증 완료 | agents/lib/call-graph.template.html, agents/lib/wiki_generator.py, CLAUDE.md | 사용자가 AX-Harness-INTG-main 콜그래프 화면 스크린샷을 제시하며 이 프로젝트의 콜그래프도 그렇게 나오길 요청 — 범위 확인 결과 사이드바 통계·범례 스타일 + 물리엔진 토글 + 툴바 전반 모두 반영하기로 확정 |
| 2026-07-23 | harness-init + wiki 생성 전체 절차 재점검(에이전트 2개 병렬 — 파이프라인 계약 감사 + wiki 생성 실제 POC) 후 발견 결함 다수 수정. **[파이프라인 감사]** (1) `skills/cross-repo-modify/SKILL.md` Phase 6 "양쪽 change-safety" 호출 2곳이 `model="opus"`로 잔존(2026-07-15 opus 하드코딩 회귀 4건 수정 시 당시 신규 스킬이라 감사 대상 누락) → `model="sonnet"` 교정. (2) `validator_checks.py` check4(경로 교차검증)가 cross-repo 스킬이 예시로 인용하는 파트너 저장소 경로를 "없는 경로"로 오탐 FAIL하던 것을 `pair_config.md`의 partner_root 기준으로도 존재 확인하도록 수정. (3) check7b 스팟체크의 `_simple_name()`이 `sql:PKG_X`/`page:Menu.vue`류 type-prefix 노드 id의 접두사를 벗기지 않아 실제 소스에 없는 문자열로 찾아 오탐 FAIL하던 버그 수정(prefix 정규식 추가). (4) check8(harness-init.md 보존 확인)이 애초에 `skills_builder.py`가 해당 파일을 프로젝트에 배포한 적이 없어(메타/툴링 스킬, writer 산출물 아님) 상시 FAIL 상태였던 모순 발견 — "존재하면 보존 확인, 없으면 정상(플러그인이 전역 제공)"으로 완화, `STATIC_OR_PREEXISTING_SKILLS`(check3)에서도 제외해 남은 사본은 오히려 트리거 품질 검사 대상이 되도록(정리 유도). `wiki_content.py`/`docsify_convert.py`의 workflows 목록·사이드바에서도 harness-init.md를 프로젝트 개발 워크플로우가 아닌 메타 스킬로 분리("AI 도구" 섹션), 워크플로우 목록 자체에서 제외. `skills/harness-init/SKILL.md` 백업 안내에서 "harness-init.md 제외" 예외 문구 제거(더 이상 배포 대상이 아니므로 예외 자체가 무의미). **[wiki POC]** 더미 프로젝트로 `wiki_generator.py --storage folder` 실제 실행 검증 — 15개 산출물(Home.md~offline.html, call-graph.html, _html/*) 전부 정상 생성, 플레이스홀더 잔존 0건, DB 모드는 `.env` 없을 때 graceful 실패 확인. 이 과정에서 `wiki_db.py`의 `load_db_to_folder`(DB→폴더 복원)가 2026-07-15 `wiki_render.render_index()`의 Docsify 전환 이후에도 옛 시그니처(`heading`/`entries`)로 호출 중이었고, `_sidebar.md`/`_navbar.md`/`serve.bat`/`offline.html`을 아예 생성하지 않아 DB 복원 시 폴더 모드와 달리 Docsify 네비게이션 없는 반쪽짜리 wiki가 나오던 버그 발견 — `docsify_convert.build_sidebar/build_navbar/serve_bat_content` + `wiki_render.render_static_index`를 재사용해 폴더 생성 경로와 동일한 산출물이 나오도록 수정 | skills/cross-repo-modify/SKILL.md, agents/lib/validator_checks.py, agents/lib/wiki_content.py, agents/lib/docsify_convert.py, agents/lib/wiki_generator.py, agents/lib/wiki_db.py, skills/harness-init/SKILL.md, CLAUDE.md | 사용자가 2026-07-22~23 harness-init 대규모 변경(Phase -1/-2 병합, Tier 로직 교체, QA/wiki 온디맨드화, 진행상황 한글표시, 절대경로 전환, Lite 폐지, ito-guide 기계화, check 7b 추가)이 wiki 생성까지 포함해 전체 절차에 일관 반영됐는지 점검·POC·수정 요청 |
| 2026-07-23 | 플러그인 버전 0.6.0 → 0.6.1 (`.claude-plugin/plugin.json`). 위 재점검 세션의 버그 수정 6건(cross-repo-modify opus 잔존, check4 partner_root 오탐, check7b 노드 id 접두사 오탐, check8 harness-init.md 상시 FAIL, wiki_db.py DB 복원 Docsify 미생성 등)을 반영한 버전 표시 | .claude-plugin/plugin.json, CLAUDE.md | 사용자가 플러그인 업데이트 요청 — 로컬 디렉터리 마켓플레이스 설치라 버전 번호는 배포 산출물이 아니라 변경 이력 추적용(2026-07-22 결정 재확인) |
| 2026-07-24 | 사용자가 harness-init 보안 분석·wiki에 OWASP Top 10 매핑이 있는지 질문 → 점검 결과 전무함을 확인(인증/인가 경로 트레이스와 harness 문서 내 시크릿 스캔만 존재, 실제 소스의 injection/XSS/SSRF 등 취약점 카테고리 매핑 없음) 후 추가 요청받아 신설. `agents/analyzer.md` Step 14.5(신규) — OWASP Top 10 (2021) 10개 카테고리별 탐지 방법 명시(A01 인가 트레이스 재사용, A03 SQL 문자열 결합/커맨드 인젝션, A06 의존성 버전만 추출하고 CVE 대조는 "확인필요"로 남김 등), 증거 없는 카테고리는 `미탐지`로 남기고 "취약점 없음"과 구분하도록 강제. 산출물 `_workspace/index/owasp_top10.json`(신규 인덱스, `docs/index-spec.md`에 스키마 문서화) — 리포트 재진술은 기존 zero-LLM 원칙에 맞춰 analyzer가 직접 쓰지 않고 `agents/lib/analyzer_index_summary.py`의 `_section_owasp()`(신규)가 카테고리별 상태·건수를 표로 생성해 Section B에 편입(2026-07-15 하이브리드 빌드 원칙과 동일). `agents/lib/wiki_content.py`의 `build_issues()`에 `owasp_json` 파라미터 추가 + `_owasp_table()`(신규)로 상태 아이콘(🔴발견/🟡확인필요/⚪미탐지)·대표 사례 포함 표를 issues.md 최상단에 배치, `wiki_generator.py`가 `owasp_top10.json`을 로드해 전달하도록 연결. `skills/harness-init/SKILL.md`·`agents/analyzer.md` 산출물 목록·용량 한도 표에도 반영. 페어 연동 시 병합 대상에서는 제외(issues.md는 저장소별 이슈라 기존부터 병합 대상 아님 — 2026-07-15 결정 재확인). 플러그인 버전 0.6.1 → 0.6.2 | agents/analyzer.md, agents/lib/analyzer_index_summary.py, agents/lib/wiki_content.py, agents/lib/wiki_generator.py, docs/index-spec.md, skills/harness-init/SKILL.md, .claude-plugin/plugin.json, CLAUDE.md | 사용자가 harness-init의 보안 분석과 생성된 wiki에 OWASP Top 10 웹 애플리케이션 10대 보안 취약점 매핑이 있는지 확인 요청 → 없음을 확인 후 추가 요청 |
| 2026-07-27 | 여러 시스템의 wiki를 버전 관리와 함께 중앙에서 열람하는 **wiki-hub** 기능 추가 — 별도 세션에서 이 저장소의 예전 버전을 토대로 먼저 진행된 작업 결과물(중간 산출물 폴더로 전달됨)을 검토해 이식. 검토 중 그 산출물 자체가 두 단계였음을 발견: 1차 시도는 DB 코드(스키마·버전관리·MSSQL 접속)를 harness 플러그인 안(`agents/lib/wiki_store.py` 등)에 직접 넣었으나, 곧이어 같은 세션에서 "harness는 여러 프로젝트에 설치되는데 DB 허브는 한 번 설치해 계속 떠 있어야 하는 서버 성격이라 안 맞는다"는 이유로 전량 폐기하고 **별도 독립 프로젝트 `wiki-hub`**(SQLAlchemy Core 단일 스키마로 MSSQL/PostgreSQL/Oracle/SQLite 4개 엔진 방언을 흡수, `wiki-hub-publish`/`wiki-hub-serve` 콘솔 명령 제공)로 재구성한 결과물이 최종본이었음 — 최종본만 이식하고 1차 시도는 반영하지 않음. **harness 쪽 변경**(이 저장소): `agents/lib/wiki_generator.py`의 `detected_types`(set) 정렬 없는 순회 버그 수정(`sorted()` 적용 — 내용이 같아도 call-graph.html 필터/범례 순서가 실행마다 달라져 나중에 wiki-hub에 발행할 때 헛된 버전이 쌓이는 문제 예방), 신규 스킬 `publish-wiki`(발행 오케스트레이터 — DB 엔진 선택·시스템 키/컴포넌트 확인·`wiki-hub-publish` 호출)·`wiki-hub`(허브 실행 오케스트레이터 — `wiki-hub-serve` 호출), `docs/wiki-hub.md`(왜 별도 프로젝트인지, 스키마 개요, 세 가지 열람 방법 비교), `generate-wiki` Phase 3.5(폴더 wiki 생성 완료 후 "중앙 허브에도 발행할까요?" 선택 질문 추가 — Y 시 `publish-wiki`로 연결), `harness-init`의 "위키 생성 선택 시" 안내를 Phase 0~3(기존)에서 Phase 0~3.5로 확장해 자동으로 이 질문까지 이어지도록 변경(harness-init 쪽에 별도 Phase를 중복 추가하지 않고 generate-wiki에 위임 — 원본 산출물은 harness-init에도 독립된 "Phase 3.7" 질문을 별도로 추가했으나, 이미 generate-wiki 내부에 있는 질문을 상위에서 다시 반복하는 것이라 판단해 단순화), `.env.example`·`.gitignore`에 wiki-hub용 필드(`WIKI_DB_ENGINE`/`PG_*`/`ORACLE_*`/`WIKI_DB_URL`/`WIKI_SYSTEM_NAME`/`WIKI_COMPONENT_KEY`/`WIKI_COMPONENT_TYPE`, `wiki_hub.db`류) 추가. **사용자가 명시적으로 v1(기존 `sync-wiki` 스킬 + `agents/lib/wiki_db.py`/`wiki_db_server.py` + `--storage db` 단일 테이블 저장)을 삭제하지 않고 그대로 보존하도록 결정** — wiki-hub는 v1을 대체하는 것이 아니라 "여러 시스템을 조직 차원에서 버전 관리와 함께 보는" 상위 계층으로 공존시킴. 이에 따라 원본 산출물의 wiki_generator.py 변경분 중 `--storage db` 옵션과 `wiki_db` import 제거 부분은 반영하지 않고 그대로 둠. **별도 프로젝트 wiki-hub**: 산출물 그대로 `E:/AI/wiki-hub`(이 저장소의 형제 디렉터리)에 신규 생성 — `wikihub/{models,store,publish,server,ui,render,config,index_extract}.py` + `tests/{test_dialect_compile,test_store_roundtrip}.py` + `pyproject.toml`/`README.md`/`requirements.txt`/`.env.example`. models.py·store.py 정독 결과 버전관리 로직(체크섬 비교 후에만 버전 증가·삭제는 표시만·되돌리기는 새 버전 추가) 결함 없음, CLI 인자가 `publish-wiki`/`wiki-hub` SKILL.md 문서와 정확히 일치함을 확인. ~~**미검증 항목**: 샌드박스 정책이 외부 산출물 코드의 `pip install`·테스트 스크립트 실행 자체를 차단(신뢰되지 않은 소스 코드 실행 금지 원칙)해 `tests/test_dialect_compile.py`·`tests/test_store_roundtrip.py` 실제 실행은 하지 못함 — 정적 코드 리뷰로만 확인.~~ **(2026-07-29 실행 검증 완료)**: `E:/AI/wiki-hub`에서 `pip install -e .` 실행(flat layout `pyproject.toml`의 `where=["src"]` 제거가 이미 정확히 반영돼 정상 설치됨) 후 두 테스트 모두 실제 실행. `test_dialect_compile.py` — 테이블 9개 × 엔진 4개(mssql/postgresql/oracle/sqlite) 컴파일 전부 통과(exit 0). 최초 실행 시 Windows 콘솔(cp949)이 마지막 요약 print의 em-dash·한글을 못 찍어 `UnicodeEncodeError`가 났으나 이는 로직 통과 이후의 print 단계 문제일 뿐 — `PYTHONIOENCODING=utf-8`로 재실행해 정상 출력과 exit 0을 재확인. `test_store_roundtrip.py` — SQLite 실접속으로 upsert/버전증가/무변경 스킵/삭제표시/되돌리기(이력 안 줄어듦)/검색/인덱스 교체/보관 처리 등 24개 체크 전부 OK, exit 0. 추가로 `E:/AI/00_sample/wiki-hub/src/wikihub/*.py`(8개 모듈)와 `E:/AI/wiki-hub/wikihub/*.py`를 diff해 바이트 단위 동일함을 재확인(포팅 누락 없음). 플러그인 버전 0.6.2 → 0.7.0 | agents/lib/wiki_generator.py, agents/lib/wiki_render.py, skills/publish-wiki(신규), skills/wiki-hub(신규), skills/generate-wiki/SKILL.md, skills/harness-init/SKILL.md, docs/wiki-hub.md(신규), .env.example, .gitignore, .claude-plugin/plugin.json, CLAUDE.md, [별도 프로젝트] E:/AI/wiki-hub/ 전체(신규) | 사용자가 별도 세션(E:\AI\00_sample\ax-std-harness + E:\AI\00_sample\wiki-hub)에서 진행된 다중 시스템 wiki 중앙 관리 기능을 이 저장소에도 반영해달라고 요청 |
| 2026-07-29 | 크로스 리포 연동에 허브형(1:N, 예: 백엔드 1개 + 웹/모바일iOS/모바일Android/관리자 등 클라이언트 2개 이상) 구조 지원 추가 — 기존엔 `pair-init`/`pair_config.md`가 1:1(백엔드+프론트엔드 1개)만 전제해 클라이언트가 여러 개인 조직 구조를 표현할 방법이 없었음. `init_layout`에 `hub-roots`(신규, 기존 `paired-roots`와 별개 값 — 사용자가 명시적으로 "1:1과 1:N 구분" 선택) 추가. **harness-init**: Phase -1 메뉴에 5번(허브형) 추가, hub-roots 선택 시 클라이언트 개수+역할 라벨(자유 입력, 예: web-frontend/mobile-ios/mobile-android/admin-panel)+경로+API URL을 반복 수집해 `partner_list`로 저장, Phase 3.5에서 pair-init에 리스트째 전달하고 하네스 없는 클라이언트 자동 생성을 harness-evaluator와 함께 병렬 실행(M개 전부 한 메시지). **pair-init**: Phase 0에 모드 판단(1:1/1:N/기존 hub-roots 설정 발견 시 "새 클라이언트 추가/특정 재설정/전체 재설정" 3지선다) 추가, Phase 1/2/4/5/6을 1:1(-A)과 1:N(-B) 갈래로 분리 — hub 쪽 `pair_config.md`만 신규 다중 블록 형식(`## Partner: <label>` 반복)을 쓰고, 각 클라이언트 쪽은 기존 1:1 flat 형식 그대로(자신 입장에선 파트너가 hub 1개뿐이므로 hub-roots를 알 필요가 없음 — 이 설계 덕분에 analyzer/writer/impact-analyzer 등 클라이언트 쪽 코드는 대부분 무변경). API 계약 추출은 여전히 백엔드 1회, 드리프트 검증은 클라이언트마다 병렬 반복. **wiki 병합**(`agents/lib/wiki_generator.py`/`wiki_content.py`): 기존 1:1 파라미터(`partner_report_text` 등)는 그대로 두고 `partners`(리스트) 파라미터를 추가로 받아 파트너 수만큼 "## 파트너 (label)" 섹션을 이어붙이는 방식으로 확장(1:1 호출부는 무변경, 1:N만 새 인자 사용) — call-graph 병합은 신규 `merge_hub_partner_call_graphs()`가 파트너마다 고유 접두사(`partner0_`, `partner1_`, ...)로 병합. **CLAUDE.md 파트너 섹션**(`skills_builder.py`): hub-roots면 클라이언트 목록 표 형식으로, 1:1이면 기존 서식 그대로 조립(같은 파싱 버그 수정 포함). **크로스 리포 스킬**: `cross-repo-scaffold`/`cross-repo-modify` 둘 다 Phase 0에서 등록 클라이언트 2개 이상이면 매번 "포함할 파트너 체크리스트" 질문(사용자가 두 옵션 중 이 방식을 명시적으로 선택 — 기본 전체 포함 방식은 채택 안 함) 후 선택된 대상 전체에 대해 스캐폴딩/영향확인/적용/드리프트재검증을 병렬 반복. **impact-analyzer** Step 8.5, **analyzer** Step 0.5도 1:N 감지 시 파트너 블록마다 반복하도록 확장. **실제 발견·수정한 버그 2건**: (1) `## Partner:` 블록 파싱에 `re.MULTILINE`과 `re.DOTALL`을 한 정규식에 같이 써서 그리디 `(.+)$`가 파일 끝까지 삼켜버려 파트너 label에 블록 전체 텍스트가 섞여 들어가던 버그 — 정규식을 버리고 줄 단위 분할로 재작성(`wiki_generator.py`·`skills_builder.py` 둘 다 동일 버그, 둘 다 수정). (2) 기존 1:1용 `parse_pair_config()`가 hub-roots 파일을 만나면 첫 번째 파트너 블록의 `partner_root`/`partner_workspace` 등을 마치 최상단 1:1 값인 것처럼 잘못 읽어 1:1 경로가 hub-roots 파일에서도 잘못 활성화되던 버그 — `## Partner:` 등장 이전 텍스트까지만 스캔하도록 제한. 두 버그 모두 합성 hub-roots `pair_config.md`(웹+iOS 2개 클라이언트)로 `wiki_generator.py` 실제 실행 후 발견해 수정, 수정 후 architecture.md 파트너 섹션 2개 정상 병합·call-graph 노드 병합·`skills_builder.build_partner_section()` 클라이언트 표 출력까지 재검증 완료. 기존 1:1 흐름도 동일한 합성 데이터로 회귀 테스트해 무변화 확인. 플러그인 버전 0.7.0 → 0.7.1 | skills/harness-init/SKILL.md, skills/pair-init/SKILL.md, skills/cross-repo-scaffold/SKILL.md, skills/cross-repo-modify/SKILL.md, agents/analyzer.md, agents/impact-analyzer.md, agents/lib/wiki_generator.py, agents/lib/wiki_content.py, agents/lib/skills_builder.py, README.md, .claude-plugin/plugin.json, CLAUDE.md | 사용자가 "백엔드/프론트엔드 2개짜리 페어 연동 외에 모바일 등 1:N 구조도 있으니 반영해달라"고 요청 — 방향 확정을 위해 (1) 기존 옵션 확장 vs 신규 옵션 추가, (2) 스캐폴딩 시 기본 전체 포함 vs 매번 체크리스트 질문 2가지를 먼저 물어 사용자가 "신규 옵션 추가"·"매번 체크리스트"를 선택한 뒤 그 방향으로 구현 |
| 2026-07-29 | v1 단일 테이블 wiki DB 저장(2026-07-14 도입, 2026-07-27엔 사용자 결정으로 wiki-hub와 공존시켰던 것) 완전 제거 — `agents/lib/wiki_db.py`, `agents/lib/wiki_db_server.py`, `agents/lib/requirements.txt`(pymssql, v1 전용), `skills/sync-wiki/` 삭제. `wiki_generator.py`의 `--storage [folder|db]` 인자와 DB upsert 분기 제거(항상 폴더 저장). `generate-wiki` Phase 1.5(저장 위치 선택 질문·시스템 키 확인)를 통째로 삭제하고 Phase 2가 바로 실행되도록 단순화, Phase 3 보고에서 "DB 저장 선택 시" 문구·sync-wiki 안내 제거. `wiki-hub`/`publish-wiki` 스킬과 `docs/wiki-hub.md`에서 "위키를 보는 세 가지 방법"(폴더/v1 DB/중앙 허브) 표를 "두 가지"(폴더/중앙 허브)로 축소, v1 공존 안내 문구 전부 제거하되 `wiki-hub-publish --migrate-v1`(예전 `harness_wiki_pages` 데이터를 wiki-hub 새 스키마로 이관하는 도구, wiki-hub 쪽 기능이라 그대로 유지) 안내는 남김. `.env.example`의 MSSQL_* 필드를 "v1과 wiki-hub가 공유"에서 "wiki-hub(WIKI_DB_ENGINE=mssql) 전용"으로 주석 정리, 중복 선언돼 있던 `MSSQL_PORT` 두 줄을 하나로 병합. **wiki-hub 쪽엔 변경 없음** — `wikihub/models.py`(systems/components/pages/page_versions/api_endpoints/db_objects/frontend_routes/external_links/publish_log 9개 테이블)가 이미 사용자가 요구한 "상세 분리 저장"(문서 본문과 API·DB·화면·외부연동을 별도 테이블로 분리, 시스템·컴포넌트 단위로 완전 분리, 버전 이력 별도 테이블)을 전부 구현하고 있어 스키마 확장 불필요함을 확인. CLAUDE.md 상단 파일 구조 표·자동 워크플로우 표에서 sync-wiki 행 제거. `.claude-plugin/plugin.json` 설명의 스킬 수(17→16) 갱신, 버전 0.7.1 → 0.7.2 | agents/lib/wiki_db.py(삭제), agents/lib/wiki_db_server.py(삭제), agents/lib/requirements.txt(삭제), skills/sync-wiki/(삭제), agents/lib/wiki_generator.py, agents/lib/wiki_render.py, skills/generate-wiki/SKILL.md, skills/wiki-hub/SKILL.md, skills/publish-wiki/SKILL.md, docs/wiki-hub.md, .env.example, .claude-plugin/plugin.json, CLAUDE.md | 사용자가 "단일 테이블 형태 저장은 걷어내고 wiki-hub로 하는데, 저장된 wiki 정보를 상세히 분리해서 저장돼야 한다"고 요청 — 점검 결과 상세 분리는 이미 2026-07-27 이식 시점부터 wiki-hub 스키마에 구현돼 있었음을 확인, 실제로 필요했던 작업은 v1 제거뿐이었음 |
| 2026-07-30 | wiki-hub 발행 시 `wiki/*.md`·`*.html` 뿐 아니라 `_workspace/**/*.json`(call_graph·schema·sql_usage·owasp_top10 등 harness index + writer_decisions.json·claude_md_fields.json·validator_mechanical.json 등)도 함께 발행·버전관리하도록 확장 — 여러 인원이 한 프로젝트를 나눠 맡을 때 한 사람이 harness-init을 돌린 분석 인덱스 원본을 다른 팀원이 harness-init 재실행(토큰 비용) 없이 그대로 받아 쓸 수 있게 하기 위함. **wiki-hub 쪽 변경**(별도 프로젝트 `E:/AI/wiki-hub`): `publish.py`에 `iter_workspace_json_files()`(project_root 기준 `_workspace/` 아래 모든 `*.json`을 `_workspace/...` 상대경로로 열거 — 이 접두사 덕분에 wiki 페이지 경로와 절대 겹치지 않아 같은 `wikihub_pages` 테이블에 스키마 변경 없이 함께 저장됨) 추가, `publish()`가 위키 페이지와 워크스페이스 JSON을 한 번에 저장(체크섬 기반 버전 관리·삭제 감지 모두 기존 로직 재사용), `--no-workspace-json` 옵션으로 건너뛸 수 있게 함. `pull()`(`--pull`, DB→폴더 복원)이 기존엔 모든 페이지를 wiki_dir 밑에만 복원해 JSON을 받아도 harness 에이전트가 읽는 위치(`_workspace/index/` 등)가 아니었던 문제를 수정 — `_workspace/`로 시작하는 페이지는 project_root 기준 원래 경로로, 나머지는 wiki_dir로 분기 복원하도록 시그니처에 `project_root` 추가. `server.py`의 시스템 상세 화면(`page_system`)에서 컴포넌트별 페이지를 "문서"와 "워크스페이스 인덱스(JSON)" 두 표로 분리해 JSON이 발행·관리되고 있음을 명시적으로 표기, `page_page()`/`/page/` 라우트에 `?raw=1` 추가(서식 없이 원본 그대로 반환 — content_type도 원본 그대로 내려줘 JSON을 로컬 파일로 받기 편함), `ui.py`에 `ctype_tag()`(MD/HTML/JSON 태그) 헬퍼 추가. 더미 프로젝트(wiki/Home.md + `_workspace/index/call_graph.json` + `_workspace/writer_decisions.json`)로 SQLite 엔진 실제 발행(3페이지, JSON 2개 포함) → `--list` 확인 → 별도 폴더로 `--pull`(JSON이 `_workspace/index/...` 원래 경로로, md는 `wiki/`로 정확히 분리 복원됨을 파일 트리로 확인) → 재발행 시 3개 전부 "동일"(체크섬 스킵) → `wiki-hub-serve` 기동 후 `/s/{시스템}` 페이지에 "워크스페이스 인덱스(JSON) 2개" 표 렌더 확인 → `?raw=1`으로 원본 JSON 바이트·`Content-Type: application/json` 헤더까지 curl로 실제 확인. 기존 `tests/test_dialect_compile.py`·`tests/test_store_roundtrip.py` 재실행해 무회귀 확인(둘 다 exit 0). **harness 쪽 변경**(이 저장소): `skills/publish-wiki/SKILL.md`에 `--no-workspace-json` 옵션·워크스페이스 JSON 발행 동작·Phase 4 보고 예시 갱신, `docs/wiki-hub.md`에 스키마 표·"여러 인원이 나눠 맡는 경우" 협업 시나리오·`?raw=1` 설명 추가. 스키마 변경은 없음(`page_path`/`content_type`가 이미 자유 문자열 컬럼이라 JSON도 그대로 수용). 플러그인 버전 0.7.2 → 0.7.3 | [별도 프로젝트] E:/AI/wiki-hub/wikihub/publish.py, E:/AI/wiki-hub/wikihub/server.py, E:/AI/wiki-hub/wikihub/ui.py, E:/AI/wiki-hub/README.md, skills/publish-wiki/SKILL.md, docs/wiki-hub.md, .claude-plugin/plugin.json, CLAUDE.md | 사용자가 "wiki를 DB에 저장할 때 _workspace 아래 json 파일도 저장해서 wiki-hub에서 관리하고 있음을 표기·열람·버전관리 했으면 한다"고 요청 — 여러 인원이 한 프로젝트를 나눠 맡을 때 개개인이 harness-init을 다시 돌리는 토큰 비용을 줄이고, git과 별개로 wiki-hub를 통해 분석 인덱스를 공유하려는 목적. 사전 확인 질문 2건(JSON 범위: `_workspace` 전체 vs `index/`만 → 전체 선택, `--pull` 복원 위치: 원래 경로 vs wiki 폴더 안 → 원래 경로 선택)에 사용자가 답한 뒤 그 방향으로 구현 |
| 2026-07-31 | harness-init Phase 4에 "인덱스 무결성 기계 게이트" 신설 — 오늘 추가한 validator_checks.py의 새 하드 게이트(dangling edge·`_meta` 누락·edge count 불일치)와 휴리스틱 WARN(import/inherit/inject 편중, generated_at 실시각 의심)이 지금까지는 `_workspace/03_validator_report.md`에 리포트만 되고 아무도 강제로 반응하지 않는 구조였음을 발견 — harness-evaluator의 4개 평가 차원은 harness *파일*이 인덱스를 "참조하는지"만 보고 인덱스 *내용*(그래프 완전성)은 채점 대상이 아니라서, qualitative 점수가 80 이상(PASS)이어도 dangling edge나 `_meta` 누락 같은 구조적 결함이 있는 인덱스가 그대로 통과될 수 있었음(오늘 mfs-test3·모바일법인카드·xuz06 3개 프로젝트 전부 최초 harness-init 시점부터 이 상태였을 것으로 추정, 아무도 리포트를 열어보기 전까진 발견 불가능한 구조). `skills/harness-init/SKILL.md` Phase 4에 "인덱스 무결성 기계 게이트" 절 추가 — Phase 2-4에서 이미 생성된 `validator_mechanical.json`의 `index_integrity_fail`/`index_spotcheck_fail`/관련 warn(2종)을 확인해, 하나라도 해당하면 harness-evaluator 점수가 PASS여도 `analyzer` 강제 재실행을 fix_targets에 추가(기존 fix_target과 병합, 새 스크립트 실행 불필요 — 이미 있는 JSON을 읽기만 함). 재실행 후 `validator_checks.py` 1회 재검증, 미해소 시 추가 재시도 없이 Phase 3 보고에 "인덱스 무결성 잔존 이슈"로 명시(2026-07-22 확립된 "1회 재시도 후 재실패 시 결과 명시" 원칙과 동일). "타겟 재생성 실행" 절 제목·에러 핸들링 표·Phase 3 보고 템플릿도 이 게이트를 반영하도록 갱신 | skills/harness-init/SKILL.md, CLAUDE.md | 사용자가 "이제 harness-init 하면 누락 없이 제대로 분석 될까?" 질문 → 오늘 추가한 기계 게이트들이 리포트만 하고 아무도 강제 반응 안 하는 구조적 허점이 있음을 확인해 알렸고, 사용자가 "전체 수정 해도 돼, 좋은 결과물만 만들 수 있으면"이라고 승인해 진행 |
| 2026-07-31 | xuz06-server(Spring Boot)·xuz06-client(Vue3, pair-linked) 재검증 중 발견한 결함 2건 수정. (1) client의 call_graph에 import 그래프 추가 지시가 1차 작업에서 누락됐던 것 발견(server는 재지시로 잡았으나 client는 재확인 없이 넘어감) — 재지시로 노드 27→51/엣지 33→92(import 59개 추가) 보완, 독립 재검증으로 dangling 0 확인. (2) 그 재검증 과정에서 `validator_checks.py`의 check7b(엣지 스팟체크)가 `import` 타입 edge(파일↔파일 관계)에도 심볼용 `_simple_name()`(마지막 `.` 이후를 심볼명으로 간주)을 그대로 적용해, 파일 경로에서 확장자("js" 등)만 뽑아 검색하는 무의미한 비교를 하고 있던 버그 발견 — `_simple_name('src/utils/request.js')` → `'js'`로 확인, 우연에 가까운 매칭이라 새로 늘어난 import edge들 때문에 갑자기 FAIL(9/20)이 뜬 것이었지 실제 데이터 결함이 아니었음. `check7b_spotcheck()`를 수정해 `import` 타입 edge는 심볼명 매칭 대상에서 제외하고, 별도로 "대상 파일이 실제 존재하는가"만 확인하는 스팟체크를 추가(더 이 edge 타입에 맞는 검증). 수정 후 xuz06-client 재검증 PASS(20/20), 이미 import edge를 가진 mfs-test3·모바일법인카드·xuz06-server 3개 프로젝트에 회귀 테스트해 전부 문제없음 확인(mfs-test3에서 미스 1건 발견했으나 20개 중 19개 통과로 임계치 이상 유지, 실제 파일 아닌 모듈 참조라 별건) | agents/lib/validator_checks.py, CLAUDE.md | 사용자가 "지금까지 확인된 오류 이 프로젝트에 제대로 반영된거야?" 질문 — 전체 재검증하다가 client의 import 그래프 누락 재발견, 그걸 고치자 이번엔 check7b 자체의 사전 버그가 새로 노출됨 |
| 2026-07-31 | 이 세션에서 만든 수정분(validator_checks.py 신규 게이트 3종·check7b import edge 버그 수정, analyzer.md Step 8 자체검증·`_meta.generated_at` 규칙, `agents/lib/now_kst.py` 신규, docs/index-spec.md, agents/validator.md, harness-init Phase 4 인덱스 무결성 기계 게이트)이 소스 저장소(`E:/AI/ax-std-harness`)에만 있고 실제 활성 플러그인 설치본에는 반영 안 된 상태였음을 확인 — `installed_plugins.json` 조회 결과 active installPath는 `~/.claude/plugins/cache/ax-std-harness/ax-std-harness/0.7.4`(git commit `1cbcec1`에 고정, 이 세션 변경분 커밋 전이라 당연히 stale)이고, 로컬 디렉터리 마켓플레이스(`known_marketplaces.json`의 `installLocation: E:\AI\ax-std-harness`)는 소스일 뿐 자동 동기화되지 않음을 재확인(2026-07-15 "다른 시스템의 플러그인 캐시" 발견 때와 동일 구조). 위 7개 변경 파일(`agents/lib/validator_checks.py`, `agents/analyzer.md`, `agents/validator.md`, `docs/index-spec.md`, `skills/harness-init/SKILL.md`, `CLAUDE.md`, `agents/lib/now_kst.py` 신규)을 캐시 경로에 직접 복사해 diff 0(바이트 동일) 확인 + 캐시 쪽 `validator_checks.py` 실제 실행해 정상 동작 확인. `.claude-plugin/plugin.json` 버전 0.7.4 → 0.7.5(소스·캐시 양쪽) — 실제 배포 산출물이 아니라 변경 이력 추적용이라는 기존 결정(2026-07-22) 그대로, 공식 마켓플레이스 업데이트 절차를 대신하는 것은 아니며 다음 정식 업데이트 시 새 캐시 폴더(0.7.5)가 별도 생성될 수 있음 | agents/lib/validator_checks.py, agents/analyzer.md, agents/validator.md, docs/index-spec.md, skills/harness-init/SKILL.md, agents/lib/now_kst.py, .claude-plugin/plugin.json, CLAUDE.md, [설치본] C:/Users/HHI/.claude/plugins/cache/ax-std-harness/ax-std-harness/0.7.4/ 동일 파일들 | 사용자가 "지금 수정한것 plugin 에도 적용해" 요청 — 소스 저장소 수정만으론 실행 중인 세션이 실제로 그 로직을 쓰지 않는다는 걸 확인한 뒤 활성 캐시에 직접 반영 |
| 2026-07-31 | wiki DB 발행(publish-wiki)이 별도 프로젝트 wiki-hub의 `wiki-hub-publish` 로컬 설치를 전제로 했는데, 대부분의 PC에는 wiki-hub가 clone·pip install 돼있지 않아 DB 저장 자체가 안 되는 문제 확인 — 사용자가 "wiki-hub 확인하지 말고 DB 저장만 제대로 되게 해라, wiki-hub는 나중에 별도 서버로 띄워 조회·관리 전용으로 쓸 계획"이라고 요청. 설계 방향 확인 질문(harness가 DB에 직접 저장 vs 설치 확인만 완화 vs 지금은 비활성화) 결과 "harness가 DB에 직접 저장"으로 확정 — 2026-07-27에 정한 "DB 코드는 harness 플러그인 안에 없다" 설계를 이번에 뒤집음(저장은 harness 내장, 조회·관리만 별도 wiki-hub 서버로 분리 유지). wiki-hub 프로젝트(`E:/AI/wiki-hub`)의 발행(쓰기) 경로에 필요한 4+1개 파일(`models.py`/`store.py`/`config.py`/`index_extract.py`/`publish.py` — server.py/ui.py/render.py 등 view 전용은 제외, 의존성도 `SQLAlchemy`뿐이고 `Markdown`은 view 전용이라 불필요함을 import 분석으로 확인)을 `agents/lib/wikihub_db/`에 그대로 옮김. 패키지 상대 import(`from . import config` 등)만 `sys.path.insert(0, 스크립트자신경로)` 부트스트랩 + 절대 import로 변경, 로직은 무변경(스키마가 원본과 어긋나면 나중에 wiki-hub-serve가 같은 DB를 못 읽으므로). `publish.py`를 `python agents/lib/wikihub_db/publish.py --root ... `로 직접 실행 가능(콘솔 스크립트 등록 불필요). `skills/publish-wiki/SKILL.md` Phase 0을 "wiki-hub-publish --help 설치 확인"에서 "python -c import sqlalchemy 확인(없으면 pip install sqlalchemy + 엔진별 드라이버 안내)"으로 교체, 모든 `wiki-hub-publish ...` 호출을 `python "$env:CLAUDE_PLUGIN_ROOT/agents/lib/wikihub_db/publish.py" ...`로 치환, "원칙" 절의 "DB 코드는 harness 안에 없다"를 새 원칙(저장=harness 내장, 조회=별도 서버)으로 교체. `skills/generate-wiki/SKILL.md`·`skills/wiki-hub/SKILL.md`·`docs/wiki-hub.md`(구조도·설치 절 포함 대폭 재작성)도 동일하게 갱신, `.env.example` 상단 설명 문구 정정. 실행 검증 — (1) SQLite로 dry-run→발행→`--list` end-to-end 통과. (2) **조직 공용 MSSQL DB(오늘 이전 세션들에서 원본 wiki-hub CLI로 이미 발행해둔 HHI_MFS/MFINANCE-CARD/SELFLEARNING/TODAYWORK/XUZ06 5개 시스템)에 이 vendored 스크립트로 `--list` 실제 접속** — 전부 정상 조회돼 스키마 완전 호환 확인(원본 CLI가 쓴 데이터를 이 사본이 그대로 읽음). 오늘 세션에서 harness 캐시에 반영해온 관례대로 이번 신규 파일 5개도 활성 플러그인 캐시(`~/.claude/plugins/cache/ax-std-harness/ax-std-harness/0.7.4/agents/lib/wikihub_db/`)에 동일하게 복사 예정(다음 단계) | agents/lib/wikihub_db/models.py(신규), agents/lib/wikihub_db/store.py(신규), agents/lib/wikihub_db/config.py(신규), agents/lib/wikihub_db/index_extract.py(신규), agents/lib/wikihub_db/publish.py(신규), skills/publish-wiki/SKILL.md, skills/generate-wiki/SKILL.md, skills/wiki-hub/SKILL.md, docs/wiki-hub.md, .env.example, CLAUDE.md | 사용자가 "wiki 생성하고 db 에 저장할 때 wiki hub 를 확인하고 있지? 대부분의 pc에는 wiki hub가 설치 안 돼있어. wiki hub 확인을 하지 말고 db 저장만 제대로 되도록 보완해. wiki hub는 별도 서비스로 서버에 구성해서 호출해 관리할 계획"이라고 요청 |
| 2026-07-31 | wiki 생성을 harness-init의 기본(자동) 실행으로 전환 — 기존엔 QA와 같은 Phase 3.6 선택 메뉴에 묶여있어 사용자가 매번 번호를 골라야만 wiki가 만들어졌음(사용자가 "harness-init 끝나면 wiki 생성이 디폴트냐"고 물어 확인하는 과정에서, 실제로는 디폴트가 아니라 선택 메뉴였음을 발견). `skills/harness-init/SKILL.md`의 Phase 3.6(QA·Wiki 통합 선택 메뉴)을 분리 — 새 **Phase 3.6: 위키 생성(기본 실행, 질문 없음)**이 `generate-wiki` Phase 0~3.5를 항상 자동 수행(내부의 "중앙 DB 발행할까요? Y/N" 질문은 그대로 유지 — wiki 생성 여부와는 별개의 선택이라 자동화 대상 아님), QA는 **Phase 3.7: 선택 작업 안내(QA)**로 남아 여전히 온디맨드(1/2 메뉴)다. 상단 "팀 구성" 요약과 Tier별 실행 구성 표·에러 핸들링 표·Phase 4 설명 등 본문 전체에 흩어져 있던 "Phase 3.6=QA·Wiki 선택" 참조 5곳을 "Phase 3.7=QA만"으로 정정(파트너 연동 분기 표의 "Phase 3.6으로" 문구들은 이미 새 wiki 자동 실행 단계를 가리키는 것이라 그대로 둠). `skills/generate-wiki/SKILL.md` frontmatter의 "harness-init 완료 후 자동 제안"도 "기본으로 자동 실행됨(질문 없음)"으로 정정 | skills/harness-init/SKILL.md, skills/generate-wiki/SKILL.md, CLAUDE.md | 사용자가 "위키 생성을 기본으로 해달라"고 명시적으로 요청 |
| 2026-07-31 | analyzer가 인덱스 `_meta.generated_at`을 실제 시각 조회 없이 채워온 문제 수정 — mfs-test3·모바일법인카드 두 프로젝트 모두 `generated_at`이 `2026-07-3*T00:00:00Z`처럼 자정 고정값이었는데, 이는 시간대 문제(앞선 wiki-hub UTC 건)가 아니라 애초에 실제 시각이 아닌 값이 채워진 것 — analyzer(LLM)가 벽시계 조회 없이 그럴듯한 타임스탬프를 지어냈기 때문. `git_commit`을 `git rev-parse HEAD`로 얻는 것과 같은 원칙을 적용 — 신규 `agents/lib/now_kst.py`(현재 KST(UTC+9) ISO-8601을 `+09:00` 오프셋으로 출력하는 1회성 헬퍼)를 만들고, `agents/analyzer.md`의 `_meta` 필수 기록 절에 "`generated_at`은 반드시 이 스크립트 실행 결과를 쓸 것, 지어내지 말 것 — 한 실행에서 생성하는 모든 인덱스 파일에 동일 값 재사용"을 명시. `docs/index-spec.md`의 예시 값도 `Z`(UTC)에서 `+09:00`(KST)으로 교체(2개 지점). 지시만으로는 재발 가능해 `agents/lib/validator_checks.py`의 `_meta_field_issues()`에 휴리스틱 WARN 추가 — `generated_at`이 `T00:00:00Z`/`T00:00:00+09:00`(자정 고정값 의심) 또는 `+09:00`이 아닌 `Z`(UTC) 표기면 -3점과 함께 `now_kst.py` 사용을 권고(형식만으로 100% 단정 못 해 FAIL 아닌 WARN). mfs-test3에 재실행해 5개 인덱스 파일 전부에서 실제로 WARN이 뜨는 것으로 기존 결함 탐지 확인, 신규 게이트(dangling/meta 필수 필드) 오탐 없음도 함께 재확인 | agents/lib/now_kst.py(신규), agents/lib/validator_checks.py, agents/analyzer.md, agents/validator.md, docs/index-spec.md, CLAUDE.md | 사용자가 앞선 wiki-hub 시간대 수정에 이어 "analyzer의 `_meta.generated_at`도 고쳐달라"고 요청 |
| 2026-07-31 | 별도 프로젝트 wiki-hub의 화면·CLI에 표시되는 모든 시각(시스템/컴포넌트 최근 갱신, 페이지 버전 이력, 발행 로그)이 UTC 그대로 노출돼 실제(한국) 시각보다 9시간 이르게 보이던 문제 수정 — `wikihub/store.py`의 `fmt_dt()`(모든 표시용 타임스탬프 포맷이 거치는 단일 지점)가 naive UTC 값을 그대로 문자열화하던 것을 KST(UTC+9, 고정 오프셋 — 한국은 DST 없어 `zoneinfo` 불필요)로 변환하도록 수정. DB 저장 자체(`utc_now()`)는 UTC 그대로 유지(표준 관행, 엔진 간 비교 일관성) — 표시 시점에만 변환하는 것이라 스키마·기존 저장값 변경 없음. `wikihub/server.py`의 라벨 2곳("(UTC)")도 "(KST)"로 정정(나머지 표시 지점은 원래 UTC 표기 라벨 자체가 없어 무라벨로 UTC를 KST처럼 오인하기 쉬웠던 것도 이번에 실질적으로 해소됨). 기존 `tests/test_store_roundtrip.py`(24개 체크) 재실행해 무회귀 확인, `fmt_dt(naive UTC 03:00)` → `12:00`(KST) 변환 정확성 별도 검증 | [별도 프로젝트] E:/AI/wiki-hub/wikihub/store.py, E:/AI/wiki-hub/wikihub/server.py, CLAUDE.md | 사용자가 "wiki 생성이나 DB 저장 시간이 한국 시간이 아닌 것 같다"고 지적 — 확인 결과 wiki-hub가 표시용 타임스탬프를 UTC 그대로 내보내고 있었음(analyzer의 `_meta.generated_at`은 별개 사안 — 실제 시각이 아닌 하드코딩성 값이라 이번 수정 범위 밖) |
| 2026-07-31 | mfs-test3 실사용 중 call_graph.json이 호출 그래프만 채우고 임포트/DI/상속 그래프를 누락한 채(analyzer.md Step 8이 3갈래를 요구하는데도) `_meta`도 없이 그대로 통과된 사례 발견 — 사후 수정(analyzer 재호출)으로는 해결했지만, 근본 원인은 validator_checks.py의 인덱스 무결성 체크(check7)가 "파일 존재·파싱 가능·노드/엣지 0 초과"만 봤을 뿐 `_meta` 필수 필드 존재, edge 참조 무결성(dangling), edge 종류 완전성은 전혀 검사하지 않았다는 것 — 그래서 harness-init 파이프라인이 이런 누락을 자동으로 못 걸러냈음. `agents/lib/validator_checks.py`에 3가지 기계 게이트 추가 — (1) `_meta_field_issues()`: `_workspace/index/*.json` 전부(core 4종 한정 아님)에 9개 필수 필드(generated_at~files_total) 없으면 FAIL. (2) `_call_graph_integrity_issues()`: call_graph.json의 모든 edge from/to가 nodes에 실존하는지(dangling 0건 강제, FAIL), `_meta.node_count`/`edge_count`가 실제 배열 길이와 일치하는지(FAIL). (3) 같은 함수에서 휴리스틱 WARN 3종 — class 노드 있는데 inherit edge 0개 / 파일 2개 이상인데 import edge 0개 / writer_decisions.json의 detected_stack에 DI 프레임워크(Spring/NestJS/FastAPI/Angular 등) 감지되는데 inject edge 0개 — 이번에 실제로 발생했던 3가지 누락 패턴과 정확히 대응. `check7_index_integrity()`가 `decisions`(writer_decisions.json)를 인자로 받도록 시그니처 확장, `main()`의 `warns` 리스트에 새 WARN들을 합류시킴(각 -3점, 기존 FAIL 버킷과 동일하게 index_fail 시 -15 재사용). `agents/analyzer.md` Step 8 끝에 "작성 후 자체 검증(필수)" 절 추가(3갈래 그래프 전부 시도했는지, dangling 없는지, `_meta` 9필드 채웠는지, 3가지 누락 휴리스틱과 동일한 조건 스스로 점검). `docs/index-spec.md` 공통 규칙에 `_meta` 필수·참조 무결성 규칙 명문화, `agents/validator.md` 체크7 표에 새 항목 반영. 합성 테스트(빈 프로젝트에 `_meta` 없음 + dangling edge 1건 + class 있는데 inherit 0건인 가짜 call_graph.json)로 3개 게이트 전부 실제 FAIL/WARN 발생 확인, mfs-test3(이미 보완된 상태)로 회귀 테스트해 오탐 0건(새 게이트 전부 통과) 확인 | agents/lib/validator_checks.py, agents/analyzer.md, agents/validator.md, docs/index-spec.md, CLAUDE.md | 사용자가 "call-graph.json 등 시스템 분석할 때 누락되는 것들이 자주 있다, harness-init 할 때 누락이 안 되도록 확실히 보완해달라"고 요청 — 직전 세션에서 mfs-test3의 call_graph.json 누락(임포트/DI/상속 그래프, `_meta`)을 사후 발견·수정한 것을 계기로, 같은 누락이 다른 프로젝트에서도 반복되지 않도록 파이프라인 자체에 기계 게이트를 추가 |
| 2026-07-30 | harness-init Phase -1(프로젝트 구성 확인) 질문이 5개 옵션(단일/모노레포/분리저장소1:1/부분범위/허브형1:N)인데, 다른 프로젝트(`E:/AI/TodayWork`)에서 실행 시 5번(허브형 1:N)이 통째로 안 나오는 문제를 사용자가 발견해 원인 조사. 플러그인 설치 상태 자체는 정상(전역 v0.7.3, 커밋 `21d3443`으로 이 저장소 최신과 일치, SKILL.md에 5개 옵션 전부 존재 확인)이었으나, 그 세션의 실제 출력이 `AskUserQuestion` 툴의 카드 UI 형태(4개 옵션 + "Type something"·"Chat about this")였던 것을 근거로 원인 특정 — `AskUserQuestion` 툴은 옵션 최대 4개까지만 지원해(툴 스키마 `maxItems: 4`), 실행 에이전트가 이 5지선다를 평문 텍스트 대신 그 툴로 렌더링하면서 5번이 조용히 잘려나간 것. SKILL.md 자체 버그가 아니라 "5개 옵션 질문을 인터랙티브 툴로 렌더링하면 안 된다"는 제약이 명시돼 있지 않아 생긴 문제. Phase -1 질문 블록 바로 위에 "`AskUserQuestion` 툴 쓰지 않고 평문 텍스트로 출력" 경고 문구 추가 | skills/harness-init/SKILL.md, .claude-plugin/plugin.json, CLAUDE.md | 사용자가 다른 프로젝트에서 harness-init 실행 시 허브형(1:N) 옵션이 안 보인다고 보고 — 플러그인 버전·커밋 확인으로 배포 문제를 먼저 배제한 뒤, 실제 세션 출력 형태(AskUserQuestion 카드 UI 흔적)를 근거로 원인 특정 |
| 2026-08-04 | 사용자가 "현재 프로젝트 plugin에도 최신화 등록되어 있어?" 질문 — 확인 결과 활성 캐시(`~/.claude/plugins/cache/ax-std-harness/ax-std-harness/0.7.5`)가 소스 저장소 최신 커밋 `ac5896b`(0.7.6, wiki DB 내장+wiki 기본화)보다 1커밋(`24ccfb1`) 뒤처져 있어 stale 확인. 사용자 승인 받아 변경 파일 13개(`.claude-plugin/plugin.json`, `.env.example`, `CLAUDE.md`, `agents/lib/wikihub_db/{config,index_extract,models,publish,store}.py`, `docs/wiki-hub.md`, `skills/{generate-wiki,harness-init,publish-wiki,wiki-hub}/SKILL.md`)를 캐시 경로에 그대로 복사해 동기화, `diff -rq`로 `.claude/settings.local.json`(로컬 설정, 무관)·`__pycache__`(gitignore 대상) 제외 완전 일치 확인. 캐시 폴더명은 기존 관례대로 `0.7.5` 유지(내부 `plugin.json` 버전만 0.7.6 표시, 실제 배포 산출물 아닌 이력 추적용 — 2026-07-22 결정 재확인) | [설치본] C:/Users/HHI/.claude/plugins/cache/ax-std-harness/ax-std-harness/0.7.5/ 13개 파일, CLAUDE.md | 사용자가 로컬 디렉터리 마켓플레이스 설치본이 소스 최신과 일치하는지 확인 요청 후, stale 확인되자 동기화 요청 |
| 2026-08-04 | call-graph.html 노드 클릭 시 우측 "선택 노드" 패널 정보를 정확·상세하게 확장 — 기존엔 `agents/lib/wiki_generator.py`의 `meta_data`가 call_graph.json 노드에서 `type`/`file`/`api`/`note` 4개만 뽑아써서, index-spec에 정의된 `line`/`signature`/`visibility`/`static`/`annotations`가 있어도 패널에 전혀 안 보였음. `meta_data`에 `line`/`signature`/`visibility`/`static`/`annotations`/`rawType`/`inDegree`/`outDegree`/`hub`/`dead`/`deadReason` 추가, edge에도 `type`(call/inject/inherit/reflect) 보존해 연결 목록에 관계 종류 표기(`edgeTag()`, reflect는 "(추정)" 취급). `call-graph.template.html`의 `showDetail()`을 뱃지(가시성·static·허브·데드코드)·시그니처 카드·애노테이션 카드·연결 수 요약(← N개 · → N개)·데드 코드 사유 카드로 확장. **부수 발견 버그 수정**: `wiki_generator.py`가 데드 코드 판정에 `dead_code_json.get("dead_code", [])`(존재하지 않는 키)를 읽고 있어 `unused_methods`(실제 스키마 키, docs/index-spec.md)를 못 찾고 데드 코드 표시(오퍼시티·☠ 카운트·패널 배지)가 항상 0건으로 죽어 있던 것을 발견해 키 교정 — 이번 요청 이전부터 있던 기존 결함. 합성 프로젝트(controller→service 호출 + service→legacy reflect 엣지 + legacy가 unused_methods에 등재)로 실제 생성·`node --check`로 JS 문법·META/edgesData 값·데드코드 카운트(0→1로 정상화)까지 전부 실행 검증. 플러그인 버전 0.7.6 → 0.7.7 | agents/lib/wiki_generator.py, agents/lib/call-graph.template.html, .claude-plugin/plugin.json, CLAUDE.md | 사용자가 "호출 그래프 노드 클릭 시 우측 노드 정보를 더 정확히 보여줬으면 한다"고 요청 |
| 2026-08-04 | 별도 프로젝트 `E:/AI/AX-index-wiki-main`(유사 harness, 시스템 분석→JSON/MD 생성 도구)과 이 저장소의 분석 파이프라인을 비교 조사(에이전트 2회 병렬 조사) — 저쪽은 `scripts/build-index.mjs`(결정론적, 정규식/텍스트 기반, LLM 미개입)가 symbols/call_graph 등 구조적 사실을 먼저 추출하고, LLM(`agents/analyzer.md`)은 그 결과를 서술(`01_analyzer_report.md`)하거나 모호한 케이스만 작은 `_ai_patch.json`(add_edge 연산만)으로 보강하는 데 그침. 반면 이 저장소는 `agents/analyzer.md`(LLM)가 소스를 직접 읽고 `_workspace/index/*.json` 9종 전부를 처음부터 작성 — 프로젝트가 클수록 analyzer 입출력 토큰이 비례해서 커지고, 그래프 완전성이 LLM 꼼꼼함에 좌우됨(2026-07-31 mfs-test3 그래프 누락 사고가 이 구조 자체의 결과였음, 지금까지는 `validator_checks.py`의 사후 탐지로만 방어). 사용자가 "토큰은 적게, 추출 품질은 더 좋게"를 요청 → **Phase 1: Java/Spring 한정 기계 추출 레이어 도입.** 신규 `agents/lib/stack_precheck.py`(pom.xml/build.gradle + Spring 마커 초저비용 감지, `_workspace/00_stack_precheck.json` 기록) + `agents/lib/index_extractor_java_spring.py`(정규식/텍스트 기반 — javalang 등 외부 파서 의존성 없음, AX-index-wiki-main의 build-index.mjs도 실제로는 완전 AST가 아닌 동일 수준임을 조사로 확인. `.java` 파일을 2-pass로 스캔해 symbols.json + call_graph.json을 결정론적으로 생성 — class/method 노드, `inherit`(extends/implements)·`inject`(`@Autowired`/`@Inject` 필드+생성자 주입)·`import`(프로젝트 내부 타입 한정)·`call`(필드 호출+`this.` 자기 호출) 엣지. 미해석 대상(외부 라이브러리 타입 등)은 추측 없이 조용히 스킵해 dangling edge를 원천 차단, `_meta` 9필수 필드는 `now_kst.py`(이번에 CLI 전용에서 `now_kst()` 함수로 분리해 재사용 가능하게 함)+`git rev-parse HEAD`로 채움). `skills/harness-init/SKILL.md`에 analyzer 호출 전 "2-0.5" 단계 추가(precheck→감지 시 추출기 실행, 미해당/실패 시 기존 흐름 그대로 폴백) + analyzer 프롬프트에 "두 파일 이미 생성됐으면 재작성 금지" 지시. `agents/analyzer.md` Step 8에 "기계 추출 결과가 있을 때" 분기 추가(재작성 대신 모호한 케이스만 보강). `docs/index-spec.md`에 기존에도 `validator_checks.py`가 검사만 하고 문서화는 안 돼있던 `import` 엣지 타입 추가(드리프트 교정) + 기계 추출 경로 설명. sql_usage/schema/transactions/external_io/env_branches/dead_code/owasp_top10은 이번 범위 밖 — 계속 analyzer가 작성(판단 필요·항목 수 적어 토큰 임팩트 작음). 합성 Java/Spring 프로젝트(Controller→Service→Repository, `@Autowired` 필드, `extends`)로 실행 검증 — 최초 구현에서 `_annotations_before()`의 오프바이원 버그로 `@Autowired`가 전혀 인식 안 되던 것 발견해 수정, 재검증 후 `validator_checks.py`의 `index_integrity_fail`/`index_spotcheck_fail` 둘 다 False(스팟체크 6/6 일치) 확인. mfs-test3(Python/FastAPI)로 회귀 테스트해 `extractor: none` 폴백 정상 확인(생성된 테스트 산출물은 삭제). 플러그인 버전 0.7.7 → 0.8.0 | agents/lib/stack_precheck.py(신규), agents/lib/index_extractor_java_spring.py(신규), agents/lib/now_kst.py, agents/analyzer.md, skills/harness-init/SKILL.md, docs/index-spec.md, .claude-plugin/plugin.json, CLAUDE.md | 사용자가 AX-index-wiki-main과 비교해 이 저장소의 MD/JSON 생성이 약해 보인다고 지적 → 비교 조사 후 "토큰은 적게, 품질은 더 좋게" 요청 — plan mode로 범위(대상 스택·1차 JSON 범위·구현 언어) 확정 후 진행 |
| 2026-08-04 | **Phase 2: 기계 추출 레이어를 C#/.NET, Python(FastAPI/Django/Flask), Vue 2/3(+Pinia), Android/Kotlin으로 확장.** 사용자가 실제 보유 스택이 Java 외에도 다양하다며 요청, 확인 질문에서 4개 전부 선택. 공통 리팩터 먼저: `agents/lib/extractor_common.py`(신규)로 java_spring의 보일러플레이트(git_commit/base_meta/write_outputs/walk_files/dedupe_edges/annotations_before/strip_noise/matching_brace/split_type_list/simple_type) 분리, java_spring은 이 모듈을 import해 쓰도록 리팩터(로직 불변, 합성 프로젝트 재검증으로 확인). `stack_precheck.py`를 `extractor` 단수 → `extractors` 배열(모노레포에서 여러 스택 동시 감지 가능, 실제 mfs-test3=Python+Vue로 검증)로 스키마 변경. 4개 신규 추출기 각각 합성 프로젝트로 검증→커밋(스택당 1커밋) — `index_extractor_csharp.py`(ASP.NET Core는 Spring과 달리 명시 애노테이션 없이 생성자 파라미터 자체가 DI 관례라 판정 로직 다르게 설계, `using`이 클래스 아닌 네임스페이스 전체를 가져오는 문법이라 import 엣지를 "실제 해석된 참조 중 네임스페이스가 다른 대상"으로 도출), `index_extractor_python.py`(중괄호 없어 들여쓰기로 블록 판정, 클래스뿐 아니라 모듈 최상위 함수도 노드화, FastAPI `Depends()`+타입힌트 생성자 주입 지원), `index_extractor_vue.py`(SFC `<script>`/`<script setup>` 분리, Options/Composition API 둘 다, Pinia `defineStore` 파일을 별도 store 노드로 취급), `index_extractor_kotlin.py`(Kotlin 클래스 헤더 자체의 1차 생성자+Hilt `@Inject constructor` DI, 상속 목록의 생성자 호출 괄호 처리). **각 구현 과정에서 실제 버그 발견·수정**: Python은 `Depends(x)`처럼 파라미터 기본값에 괄호가 있으면 한 줄짜리 정규식이 깨져 함수 자체가 통째로 누락되는 버그(괄호 깊이 계산 방식으로 재설계) + 파라미터 타입힌트로 해석되는 지역 변수의 메서드 호출 누락. Vue는 노이즈 제거 단계가 문자열 리터럴까지 공백 치환해버려 import 경로 자체가 지워지던 버그(주석만 제거하는 별도 패스로 분리). Kotlin은 클래스 본문 시작 중괄호를 두 번 계산하면서 잘못 덮어쓰던 버그 + 1차 생성자의 val/var 파라미터(Kotlin 자동 프로퍼티)가 필드 목록에 안 잡혀 흔한 DI 호출 패턴이 누락되던 버그. **다중 스택 오케스트레이션 버그도 사전에 발견해 수정**: `write_outputs()`가 항상 파일을 덮어써서 모노레포(예: Python 백엔드+Vue 프론트엔드)에서 두 번째 추출기가 첫 번째 결과를 지워버리는 문제 — 기존 symbols.json/call_graph.json이 있으면 병합하도록 수정(합성 Python+Vue 모노레포로 실제 검증). `skills/harness-init/SKILL.md` 2-0.5를 "extractors 배열 순회, stack→스크립트 매핑 표" 방식으로, `agents/analyzer.md` Step 8과 `docs/index-spec.md`도 5개 스택 전부 반영하도록 갱신. iOS/Swift(DI 관례 약함+Storyboard 복잡도)·Vue Router/Android nav_graph.xml(리소스 파일 파싱 필요)은 이번 범위 밖으로 명시. 전 항목 `validator_checks.py`의 `index_integrity_fail`/`index_spotcheck_fail` 둘 다 False로 검증 완료. 플러그인 버전 0.8.0 → 0.9.0 | agents/lib/extractor_common.py(신규), agents/lib/index_extractor_java_spring.py, agents/lib/index_extractor_csharp.py(신규), agents/lib/index_extractor_python.py(신규), agents/lib/index_extractor_vue.py(신규), agents/lib/index_extractor_kotlin.py(신규), agents/lib/stack_precheck.py, agents/analyzer.md, skills/harness-init/SKILL.md, docs/index-spec.md, .claude-plugin/plugin.json, CLAUDE.md | 사용자가 "java 말고도 vue, c#, 모바일 등 다양한 프로젝트가 있다, 이것들도 적용했으면 한다"고 요청 — 확인 질문(대상 스택 다중선택)에서 C#/.NET·Vue·모바일(Android만 우선)·Python 4개 전부 선택 |
| 2026-08-04 | 이 개발 PC의 플러그인 설치 방식을 directory 마켓플레이스(`E:\AI\ax-std-harness` 소스 직결)에서 **GitHub 마켓플레이스(`neoruler001/ax-std-harness`)로 전환** — 다른 프로젝트에서 harness-init 실행 시 `$env:CLAUDE_PLUGIN_ROOT`가 소스 저장소 경로로 풀려 다른 PC(GitHub 설치)와 실행 형태가 달랐던 것을 사용자가 지적, 이 PC에서도 동일하게 동작하도록 요청. `claude plugin marketplace remove` → `add neoruler001/ax-std-harness` → `install`로 재설치, `installed_plugins.json`이 GitHub 소스·캐시 경로(`~/.claude/plugins/cache/ax-std-harness/ax-std-harness/0.9.0`)·commit `20a06ea`를 가리킴을 확인, 캐시 내용이 소스와 동일(줄바꿈 차이만)함을 `diff --strip-trailing-cr`로 검증. **이후 워크플로우 변경**: 소스 수정분을 활성 플러그인에 반영하려면 기존의 "캐시 폴더에 파일 직접 복사" 관례(2026-07-31~08-04 여러 회) 대신 **git push 후 `claude plugin update ax-std-harness`** — 수동 복사는 더 이상 하지 않는다 | [로컬 설정] known_marketplaces.json, installed_plugins.json, CLAUDE.md | 사용자가 "이 PC에서도 plugin 방식으로 다른 프로젝트 수행 시 다른 PC와 동일한 방식으로 되도록 수정" 요청 — 사전 전체 점검으로 플러그인 코드 자체는 하드코딩 경로 0건·전부 CLAUDE_PLUGIN_ROOT 경유임을 확인했고, 남은 차이는 이 PC의 마켓플레이스 등록 형태뿐이었음 |
| 2026-08-06 | 스킬 트리거 확장 3종 — (1) 축약 호출 등록: analyze-impact("영향도/임팩트 [대상]"), safe-modify("안전수정"), scaffold-feature("스캐폴드"), plan-migration("마이그"), review-sql("SQL리뷰/쿼리리뷰") — 배포 템플릿(agents/lib/skills/*.md.template)과 플러그인 전역(skills/*/SKILL.md) 양쪽 description에 동일 반영. (2) 범용 문구 등록: "수정해줘/고쳐줘/개선해줘/버그 잡아줘"는 safe-modify, "개발해줘/구현해줘/코드 짜줘/새 기능 만들어줘"는 scaffold-feature가 기본 경로임을 description에 명시 — 기존엔 한정 표현("컨벤션 준수", "안전하게")만 등록돼 범용 요청이 스킬을 안 타고 바로 바이브코딩되던 커버리지 구멍 해소. (3) 신규 `vibe` 스킬(템플릿+전역, 항상 배포) — "알아서 해줘/그냥 해줘/바이브로" 시 영향도 분석·안전성 평가 게이트를 명시적으로 생략하고 즉시 수정(외과적 변경 원칙 유지, 위험 변경 4종 — 스키마/API 계약/트랜잭션/3개 이상 파일 — 은 safe-modify 승격 제안). skills_builder.py ALWAYS_DEPLOY·02_writer_files 목록·ITO_SKILL_ORDER, validator_checks.py STATIC_OR_PREEXISTING_SKILLS(check3 채점 제외), ito_guide.md.template SKILL:vibe 블록, claude_md.md.template 자동 워크플로우 표(범용 2행+vibe 행+축약 호출 안내)에 연결. 플러그인 버전 0.9.0 → 0.9.1 | agents/lib/skills/*.md.template(5종 수정+vibe 신규), skills/*/SKILL.md(5종 수정+vibe 신규), agents/lib/skills_builder.py, agents/lib/validator_checks.py, agents/lib/ito_guide.md.template, agents/lib/claude_md.md.template, .claude-plugin/plugin.json, CLAUDE.md | 사용자가 (1) 등록 스킬의 축약 단어 호출, (2) 범용 문구("개발해줘" 등) 등록, (3) 반대로 AI가 알아서 하도록 하는 별도 문구 등록을 요청 — 범용 요청은 게이트 경로가 기본, 게이트 생략은 명시적 문구로만 |
| 2026-08-06 | 스킬 트리거 체계 참조 문서 `docs/skill-triggers.md` 신규 작성 — 트리거 메커니즘 2겹(description 자연어 매칭 + CLAUDE.md 자동 워크플로우 표, deterministic hook 아님·보장 경로는 슬래시 호출), 두 층 구조(플러그인 전역 ↔ 배포 템플릿, 양쪽 동기화 원칙), 등록 트리거 전체 표(축약/범용/vibe/기존), 라우팅 설계(게이트 기본값·opt-out은 명시 문구만), vibe 규칙·승격 조건·설계 근거, 새 정적 스킬 추가 시 수정 지점 10개 체크리스트(ito_guide 블록 누락 시 조용히 스킵되는 함정·check3 제외 목록 누락 시 오탐 FAIL 함정 포함), 검증 절차(더미 skills_builder + validator_checks 실행)를 기록. 플러그인 버전 0.9.1 → 0.9.2 | docs/skill-triggers.md(신규), .claude-plugin/plugin.json, CLAUDE.md | 사용자가 직전 트리거 확장 작업 내용을 추후 개발 시 참고할 수 있게 상세 문서로 남기길 요청 || 2026-08-12 | **인덱스 JSON 생성을 upstream의 결정론적 인덱서(`build-index.mjs`)로 전환.** 이 저장소는 `Malburi/harness-ito`(현 harness-sm)에서 커밋 `bbd47f7`로 갈라진 fork이고, upstream은 그 사이 인덱싱을 Node 기반 결정론적 추출(npm 의존성 0)로 발전시켰다. 비교 결과 — 이쪽은 `symbols`/`call_graph` 2종만 기계 생성(5개 스택 한정)이고 나머지는 전부 analyzer(LLM) 작성인 반면 upstream은 9종을 결정론적으로 만들고 JSP·ASP·VB·COBOL 등 ITO 레거시 스택까지 커버한다. 결정적 근거 2가지: (1) upstream 출력에 이 저장소의 `check7b` 스팟체크를 돌리면 20/20 PASS로 품질이 LLM 경로보다 낮지 않다. (2) 반대로 이쪽 `index_extractor_java_spring.py`는 실제 레거시 Java 601파일에서 20분 내 완주하지 못하고(`METHOD_DECL_RE` 백트래킹 + ctor 이중 루프), `extractor_common.strip_noise`는 `"http://a/b"` 같은 문자열 안의 `//`를 주석으로 오인해 그 줄 나머지를 통째로 지운다 — 주력 시장에서 동작하지 않는 상태였다. **결정(사용자 확인)**: mjs 이식, Python 추출기는 node 없는 환경용 폴백으로 유지하되 Vue만 주 경로에서도 계속 사용(upstream은 `.vue`를 JS로만 읽어 컴포넌트·스토어 노드와 `import`/`inject` 엣지를 못 만들어 wiki 화면 목록이 빈다), analyzer는 `_ai_patch.json`(기존 노드 사이 엣지 추가만) 프로토콜로 전환. **이식분 패치** — `_meta` 9필드·KST 타임스탬프·`api_contract` 단수(배포된 `pair_config.md`가 이 경로를 박고 있어 반대로 가면 기존 페어 연동이 끊긴다)·analyzer가 쓴 인덱스는 재인덱싱해도 삭제 안 함·`wiki/` 자기 출력 재인덱싱 방지·`extends`/`implements`를 `inherit` 엣지로 해석·데드코드 진입점 화이트리스트·엣지 dedupe를 in-degree 계산보다 먼저(그전엔 `_meta.edge_count`가 중복 포함 길이라 count 불일치 FAIL). **실측에서 발견해 고친 결함** — (a) `def f(x = Depends(g)):`처럼 기본값에 괄호가 있으면 한 줄 정규식이 첫 `)`에서 끊겨 **FastAPI 라우트 핸들러가 전부 그래프에서 누락**(mfs_backend 노드 56→79). (b) 국내 SI에 흔한 자체 쿼리 컨테이너(`<query><id>X</id><value><![CDATA[...]]></value>`)를 아무 어댑터도 몰라 레거시 SQL의 90%가 누락(519→5,352건). (c) 매퍼 정규식이 HTML/JSP의 `<select id="cmbLanguages">` 드롭다운을 SQL로 잡던 것(허위 516건 제거). (d) `_unresolved.jsonl`이 169MB(레코드 18.6만)까지 커지고 "배치 200으로 전부 처리" 계약이 930회라 성립하지 않던 것 — 레코드는 전수 유지하되 후보 목록은 판정 대상(후보 적은 순 2,000건)에만 싣고 계약을 우선순위 부분 처리로 명시(50MB). (e) `validator_checks._read`가 CP949 레거시 소스에서 `UnicodeDecodeError`로 죽던 것. (f) wiki 크로스 리포 엣지가 **항상 0건**이던 것 — 노드 텍스트에서 경로를 긁는 방식이라 경로가 안 담긴 그래프에선 매칭이 불가능했다. `api_contract.json`의 `consumers`(method·path·file·line)로 바꿔 25건 연결, 프론트 메서드→실제 백엔드 핸들러까지 정확히 지목(`AdminView.fetchUsers` → `app.api.admin.list_admin_users`). **validator 정합화** — CORE는 `call_graph`/`symbols`만(나머지는 조건부 생성이 정상이므로 `_meta.json`의 `indexes` 선언과 대조), `_` 접두 제어 파일은 9필드 규칙 제외, `import` edge 부재 WARN 제거(심볼 그래프는 이 엣지를 안 만드는 게 정상), check7b의 import 브랜치를 경로형/심볼형으로 분기(Java·Kotlin·C#에서 나던 상시 오탐 제거), 중복 node id FAIL 추가, `write_outputs`에 노드 id dedupe + 원자적 쓰기. **실증(V11)** — 레거시 Java/JSP **4,883 소스파일을 37초에 완주**(기존 Python 경로는 601 .java도 20분 초과 미완료), 노드 33,709·엣지 15,370(기존 LLM 인덱스는 356/782), 세 프로젝트(mfs_backend·mfs_frontend·xu25) 전부 check7·check7b PASS, 테스트 12/12. 플러그인 버전 0.9.2 → 0.10.0 | agents/lib/build-index.mjs(신규 이식), agents/lib/tests/(신규), docs/index-schema/(신규 10종), agents/lib/validator_checks.py, agents/lib/extractor_common.py, agents/lib/wiki_generator.py, agents/lib/wiki_content.py, agents/lib/analyzer_index_summary.py, agents/analyzer.md, agents/validator.md, skills/harness-init/SKILL.md, docs/index-spec.md, .claude-plugin/plugin.json, CLAUDE.md | 사용자가 "harness-sm과 비교해서 json 생성할 때 mjs로 하는 부분 가져오는 게 더 낫지 않냐"고 물어 비교 조사 후 방향 3건(이식 방식·Python 추출기 처리·analyzer 계약)을 확인받고 진행 |
| 2026-08-12 | 저장소를 `Malburi/ax_std_new`로 옮기면서 `marketplace.json`/`plugin.json`의 owner·author·homepage·repository·`source.repo`만 바꾸고 사용자 대면 문서(`README.md`의 `/plugin marketplace add` 명령, `docs/user-guide.md`의 설치 명령·GitHub 링크)를 빠뜨렸던 것 발견 후 수정. `README.md`의 `neoruler001/harness-new` 언급은 이 저장소 주소가 아니라 별개 프로젝트(기반이 된 4-에이전트 파이프라인)에 대한 출처 표기라 그대로 둠. 플러그인 버전 0.10.0 → 0.10.1(`claude plugin update`가 버전 문자열 기준으로 갱신 여부를 판단해, 커밋만 바뀌고 버전이 그대로면 로컬 캐시가 새 커밋을 못 받아온다는 것을 이번에 확인 — 문서만 바뀐 변경이라도 버전을 올려야 설치본에 반영됨) | README.md, docs/user-guide.md, .claude-plugin/plugin.json, CLAUDE.md | 사용자가 "readme.md에 제대로 변경 안되어있는데?"라고 지적 |
| 2026-08-12 | harness-sm의 harness-init **오케스트레이션 로직**(인덱서 자체는 이전 세션에 이식 완료)을 비교해 가치 있는 부분만 선별 이식. **가져오지 않은 것**(사용자 확인) — Tier 자동 결정(복잡도 점수)과 "인덱스 실패 시 폴백 없이 즉시 레인 중단"은 2026-07-22·07-23에 이미 명시적으로 버린 결정이라 범위 제외. **가져온 것 3가지**: (1) `agents/lib/ai-budget.mjs`(신규) — AI 호출을 role당 initial 1회로 스크립트가 강제(exit 1), harness-init 2-0.5 뒤 Step F에서 init(`--initial 3 --retries 2`, Lite 폐지로 3역할·Phase 4가 analyzer+writer 동시 재시도 가능해 2), 2-1/2-2/2-3 Agent 호출 직전과 Phase 4 재실행 직전에 claim 삽입(실패 시 하드 중단 — 이 저장소의 통상적인 "WARN 후 계속"의 예외). (2) `agents/lib/validate-harness.mjs`(신규, 축소 이식) — `docs/index-schema/*.json` 대조 JSON 스키마 검증 + call_graph 참조 무결성 + 증거 파일 존재만 남기고, analyzer/writer/pattern-extractor 리포트의 마크다운 프로즈 구조 검사(21개 H2 섹션 등)는 전부 제거(이 저장소 analyzer.md가 완전히 다른 섹션 체계 `Phase A~D`를 써서 매핑 근거 없음 — 실측 확인). 파일 존재·보안 regex 등 validator_checks.py와 겹치는 항목도 이식 안 함(대체 아니라 병행). `INDEX_SCHEMAS`의 `api_contracts`→`api_contract`(단수) 패치. validator.md에 체크 11(인덱스 스키마 검증)로 편입, FAIL -15(단 `plugin_contract_failures`는 플러그인 결함으로 감점 없이 별도 표기). (3) 페어/허브 동시 초기화 — upstream은 분리 저장소 시 두 레인(백엔드/프론트)이 처음부터 동시에 analyzer→writer→validator를 진행해 배리어 합류(소요시간 ≈ max(양쪽))하지만, 이 저장소는 자기쪽 Phase 2 전체가 끝난 뒤에야 Phase 3.5에서 파트너 생성을 시작했다(실제 코드 대조로 확인). upstream의 "매 단계마다 두 프로젝트를 직접 지휘하는 lock-step 다중 레인" 대신 더 외과적인 "조기 발사+늦은 join" 방식 채택 — Phase -1 직후 파트너 하네스 없음을 확인하면(Test-Path, AI 없음) pair-init의 기존 "파트너 하네스 자동 생성" Agent 템플릿을 그대로 참조해 `run_in_background`로 즉시 발사하고 자기쪽 Phase 0~4는 완전히 그대로 진행, Phase 3.5는 발사가 아니라 join으로 재정의. `pair-init.md`는 호출 시점이 두 곳이라는 안내 한 줄만 추가, 로직 무변경. 호스트가 background 미지원이면 조기 발사를 스킵하고 Phase 3.5가 기존 방식으로 자동 폴백(회귀 없음). **부수 발견·수정**: `docs/index-schema/*.json` 3건의 진짜 스키마 결손을 이번 실측 대조로 발견 — `symbols[].extends`가 string만 허용해 Python 추출기가 명시하는 `null`을 항상 FAIL 처리(→ `string|null`), `call_graph edges[].type` enum에 우리가 의도적으로 계속 쓰는 `import`가 누락(upstream 인덱서는 이 타입을 안 만들어서 원래 없었음), `sql_usage sqls[].type` enum에 이전 세션에서 SQL 컨테이너 어댑터에 추가한 `merge`가 누락(당시 스키마 갱신 빠뜨림 — xu25 실제 실행에서 45건 FAIL로 발견). **추가로 발견해 포함**: Phase -1 프로젝트 구성 질문을 2026-07-30 이후의 "5지선다 평문+텍스트입력"에서 2단계 `AskUserQuestion`(1차 4지선다, "기타" 선택 시 2차 2지선다)으로 전환 — 그 당시 5개를 한 질문에 담았다가 5번(허브형)이 조용히 잘려나간 실사고를 다시 만나지 않도록 매 단계 4개 이하만 노출, 호스트 미지원 시 기존 평문 방식 폴백. 검증: 신규 유닛테스트 12개 포함 전체 24/24 통과, ai-budget 양성/음성(role-once·reason 필수·소진) 전부 exit 코드까지 확인, mfs_backend·mfs_frontend·xu25 세 프로젝트 실제 인덱스에 validate-harness.mjs 실행해 스키마 패치 전 61건 FAIL(xu25 45+mfs_frontend 16) → 패치 후 전부 failures:0 PASS. Stage C(페어 동시 진행)는 SKILL.md 프로즈 변경이라 문서 정합성 검토로 검증(실제 harness-init 풀 실행은 에이전트 비용이 커 이번엔 생략, 필요 시 사용자가 실제 프로젝트로 검증). 플러그인 버전 0.10.1 → 0.11.0 | agents/lib/ai-budget.mjs(신규), agents/lib/validate-harness.mjs(신규), agents/lib/tests/ai-budget.test.mjs(신규), agents/lib/tests/validate-harness.test.mjs(신규), docs/index-schema/{symbols,call_graph,sql_usage}.schema.json, skills/harness-init/SKILL.md, skills/pair-init/SKILL.md, agents/validator.md, .claude-plugin/plugin.json, CLAUDE.md | 사용자가 "초기화 하는 부분도 다 가져오는게 좋을것 같은데 어떤거같아?"라고 물어 harness-init 오케스트레이션 전체를 비교 조사 후, Tier/fail-loud 반영 여부와 validate-harness 문서검사 처리 방향을 먼저 확인받고("ai-budget+validate-harness만", "문서검사는 잘라내고 스키마만") 진행 — 이어서 "병렬로 하는거나 선택해서 하도록 하는 부분들은?" 추가 질문에 답하며 페어 동시 초기화(포함 결정)와 Phase -1 클릭 선택 전환(2단계 방식)까지 범위에 편입 |
| 2026-08-13 | 정적 워크플로우 스킬 6종(analyze-impact/safe-modify/scaffold-feature/vibe/plan-migration/review-sql)의 대상 프로젝트 로컬 배포를 완전히 제거하고 플러그인 전역판(`skills/<name>/SKILL.md`) 단일 소스로 전환. 사용자가 "harness-init으로 생성되는 스킬들이 플러그인 스킬과 중복되는 것 같다"고 지적해 실제로 두 파일(`skills/safe-modify/SKILL.md` 140줄 vs 배포되던 `agents/lib/skills/safe-modify.md.template` 19줄)을 비교한 결과, "같은 내용"이라던 기존 문서(`docs/skill-triggers.md`)의 서술과 달리 배포본은 축약된 스텁이었고 "상세 로직은 `.claude/agents/change-safety.md` 참조"라는 문구가 가리키는 파일 자체가 애초에 대상 프로젝트에 배포된 적이 없어(`domain-expert.md`만 실제로 복사됨) 죽은 참조였음을 발견. 이 6종은 프로젝트별로 달라지는 내용이 전혀 없어(2026-07-14에 이 이유로 LLM 재작성 대신 템플릿 복사로 전환했던 것들) 로컬 사본을 둘 이유가 없다고 판단 — 플러그인이 설치된 이상 전역판이 어느 프로젝트에서든 그대로 트리거되고(이미 `build-index.mjs`/`validator_checks.py` 등 하네스 핵심 스크립트 전부가 플러그인 설치를 전제하므로 새로운 의존성도 아님), CLAUDE.md 자동 워크플로우 표가 스킬 *이름*만 가리키면 라우팅이 그대로 성립하기 때문. **단순 배포 중단만으로는 새로 깨지는 지점 2개를 함께 보정**: (1) `validator_checks.py`의 `check2_skill_registration()`이 "이 3개는 로컬에 항상 존재해야 함" FAIL 판정이었던 것을 CLAUDE.md 자동 워크플로우 표 등록 여부 판정으로 전환(로컬 파일 체크 자체를 제거하지 않았다면 모든 프로젝트에서 상시 FAIL이 났을 것). (2) `skills_builder.py`의 `deploy_ito_guide()`가 "로컬 파일 존재 여부"로 이 6개 스킬의 ito-guide.md 섹션 포함을 게이팅하고 있어(`_build_scenarios()`의 review-sql 시나리오 포함 여부도 동일), 배포를 멈추면 스킬 자체는 정상 작동하는데도 사용 설명서에서만 섹션이 조용히 사라지는 상태였던 것을 발견 — 게이팅 로직을 "이름이 `ALWAYS_AVAILABLE_SKILLS`에 있으면 항상 포함, `CONDITIONAL_SKILLS`면 `decision_for()` 판단 따름, 그 외(trace/find-logic/scaffolder/cross-repo-*)는 기존 파일-존재 체크 유지"로 교체. `agents/lib/skills_builder.py`의 `ALWAYS_DEPLOY`/`CONDITIONAL_DEPLOY`/`TEMPLATE_DIR`/`_deploy()`를 제거하고 `ALWAYS_AVAILABLE_SKILLS`/`CONDITIONAL_SKILLS`(가용성 판단 목록으로 의미 전환)로 대체, `render_writer_files_report()`의 "생성"/"미생성" 문구를 "적용"/"미적용"으로(파일 생성이 아니라 프로젝트 적용 여부이므로) 수정. `agents/lib/skills/*.md.template` 6개 파일 삭제(더 이상 아무도 읽지 않음). `agents/validator.md`(체크2 표에 누락돼 있던 `vibe` 행도 이번에 추가) · `agents/writer.md` · `skills/harness-init/SKILL.md`(자동 트리거 조건의 `analyze-impact.md` OR 절 제거 — 이제 어떤 프로젝트에도 존재하지 않아 조건이 상시 참이 될 뻔했음, 기존 하네스 버전 감지 표의 `analyze-impact.md` 존재 행도 계속 배포되는 `ito-guide.md` 존재로 교체, Phase 3 완료 보고 템플릿의 로컬 경로 나열 제거) · `docs/skill-triggers.md`(두 층 구조 절을 단일 소스 구조로 재작성, 신규 정적 스킬 추가 체크리스트 10단계→9단계로 축소) · `README.md`·`docs/user-guide.md`(디렉터리 트리·git 커밋 권장 표에서 6개 로컬 경로 제거, 실제 로컬 파일인 trace/scaffolder/find-logic로 교체)도 함께 갱신. `skills/harness-clean/SKILL.md`(글롭 기반 감지라 과거 배포분 잔존 정리 용도로 그대로 유지)·`agents/qa.md`/`qa_boundary6.py`(이미 "검사 대상 아님"으로 우아하게 저하 처리 중, on-demand라 스코어링 영향 없음)는 의도적으로 무변경. 플러그인 버전 0.11.0 → 0.11.1 | agents/lib/skills_builder.py, agents/lib/skills/*.md.template(삭제), agents/lib/validator_checks.py, agents/validator.md, agents/writer.md, skills/harness-init/SKILL.md, docs/skill-triggers.md, README.md, docs/user-guide.md, .claude-plugin/plugin.json, CLAUDE.md | 사용자가 harness-init으로 생성되는 스킬이 플러그인 자체 스킬과 중복되는 것 같다고 지적, 실제 비교로 죽은 참조를 가진 축약 스텁이었음을 확인한 뒤 "실제로 고쳐줘"라고 요청 |
| 2026-08-13 | 콜 그래프(call-graph.html) 결함 2건 조사 후 수정. **(1) DB 테이블 누락**: 사용자가 "콜 그래프 생성할 때 DB가 빠져있다"고 지적 — 조사 결과 `wiki_generator.py`는 `db_table` 색상·모양·범례·필터 버튼을 전부 이미 갖추고 있었지만(2026-06-30 콜그래프 디자인 개편 때 준비된 것), 그 입력을 실제로 만드는 곳이 없었음. 결정론적 인덱서(`build-index.mjs`)는 테이블 정보를 `schema.json`에만 쓰고 `call_graph.json` 노드로는 절대 만들지 않으며(설계상 분리), `wiki_generator.py`도 그 두 파일을 콜 그래프에 병합하는 로직이 없어 "렌더러는 준비됐는데 아무도 데이터를 안 주는" 상태였음(`agents/analyzer.md`의 LLM 경로도 `db_table` 노드 지시가 아예 없어 동일). `call_graph.json`이나 그 스키마는 건드리지 않고(다른 소비자·인덱스 계약에 영향 없도록) `wiki_generator.py`에 `merge_db_tables_into_graph()`(신규)를 추가해 렌더링 직전 메모리상의 그래프에만 `schema.json.tables[]` → `db_table:<테이블명>` 노드, `sql_usage.json`의 `sqls[].tables`+`usages[].method`(DAO 호출자, 이미 call_graph 노드 id와 일치) 조합 → DAO→테이블 `query` 타입 엣지를 결정론적으로 합성. `call-graph.template.html`의 `EDGE_TYPE_LABEL`에 `query: 'DB 쿼리'` 추가해 정보 패널에도 표기, `07_wiki_build.md`에 병합 건수 보고 추가. **(2) 대량 그래프 로딩 지연**: "노드·엣지 많으면 로딩 너무 느려" 지적 — 조사 결과 `call-graph.template.html`이 전체 노드/엣지를 인라인으로 그대로 삽입하고 물리엔진(`forceAtlas2Based`, 안정화 250회 반복)이 항상 켜진 채로, 가장 비싼 엣지 스무딩 모드(`smooth:'dynamic'`)까지 전역 적용되고 있어 실제 SI 레거시 프로젝트(2026-08-12 이식 검증 때 나온 노드 33,709·엣지 15,370 규모 등) 수준에서 병목이 되는 구조였음(캡·클러스터링·hideEdgesOnDrag 전무 확인, 반면 `build-index.mjs`엔 `REPRESENTATIVE_FILE_LIMITS`/`DIGEST_LIMITS` 등 대량 데이터 상한 선례가 이미 존재). `wiki_generator.py`에 노드 2000개 상한(연결 많은 노드·데드코드 후보·DB 테이블 우선순위 유지, 나머지는 잘라내되 화면 배너 + `07_wiki_build.md` 양쪽에 몇 개 중 몇 개인지 명시 — "no silent caps" 원칙) + 노드·엣지 수 기반 물리엔진/동적 엣지스무딩 기본값 계산(노드 300개·엣지 500개 초과 시 자동 OFF) 추가, `call-graph.template.html`에 `hideEdgesOnDrag`/`hideEdgesOnZoom` 추가 + `stabilizationIterationsDone` 시점에 물리엔진을 자동으로 꺼서(토글 버튼 상태도 동기화) 안정화 이후 불필요한 재계산이 계속되지 않게 함. 실제 실행 검증 — 소규모 더미(노드 3+테이블 1, DB 병합 결과 노드 4·쿼리엣지 1 확인)와 대규모 더미(노드 2500개 합성)로 각각 `wiki_generator.py` 실행 후 `07_wiki_build.md`·call-graph.html 산출물 확인(대규모 케이스: 노드 2500→2000·엣지 2499→1999로 절단, 배너 문구·물리엔진 기본 OFF·동적 스무딩 OFF 전부 정상 반영), 생성된 call-graph.html의 `<script>` 전체를 vis-network 스텁으로 Node에서 실제 실행해 2000개 `mkNode()`+1999개 `edge()` 호출까지 런타임 에러 없음 확인. 기존 회귀 테스트 24개 재실행해 무회귀 확인. 플러그인 버전 0.11.1 → 0.11.2 | agents/lib/wiki_generator.py, agents/lib/call-graph.template.html, .claude-plugin/plugin.json, CLAUDE.md | 사용자가 "콜 그래프 생성할 때 DB가 빠져있고 노드와 엣지가 많을경우 너무 로딩이 느려. 검토하고 수정보완해야해"라고 요청 |
| 2026-08-13 | 사용자가 harness-init Phase 1 작업 그래프를 직접 제안 — (1) 단일/모노레포에서 결정론적 인덱싱(MJS)을 `T-A` 내부의 조용한 스크립트 단계(2-0.5)가 아니라 `T-I`라는 눈에 보이는 별도 작업으로 분리, (2) 분리 저장소(paired-roots/hub-roots)를 2026-08-12에 도입한 "조기 발사 + 늦은 join"(파트너 쪽 harness-init 전체를 subagent 1개에 통째로 위임, 진행 상황 불투명, 배리어 없음) 대신 upstream 원안에 가까운 **대칭 2-레인**(`B-I→B-A→B-W→B-V` / `C-I→C-A→C-W→C-V`, hub-roots는 클라이언트 수만큼 `C1..CM`)으로 전환하고 `P-BARRIER`(양쪽 validator 확인) → `P-PAIR`(연동) → `P-REFRESH`(계약·미매칭 갱신)로 합류, 실패한 레인만 재개(`pair_state: PENDING`), (3) `T-V` 표시 이름을 실제 체크 대부분이 기계 검증이라는 걸 드러내도록 "MJS validator"로. 비교 결과 재시도 작업 제목(`T-A-RETRY` 등)은 이미 동일했고, 제안엔 `T-P`(pattern-extractor)·`T-E`(harness-evaluator)가 빠져 있어 그대로 유지하기로 확인 후 진행(사용자 확인 4건 — 대칭 2-레인 전환/T-I 분리/T-P·T-E 유지/T-V 이름만 변경 — 전부 채택). **구현**: `skills/harness-init/SKILL.md` — Phase 1 그래프에 `T-I`(blockedBy 기준점) 추가 + 분리 저장소 전용 그래프 신설(레인 실패 시 성공 레인 보존, 재개는 실패 지점부터); Phase -1의 "파트너 하네스 조기 발사" 절을 `_workspace/pair_lane_state.md`(신규 — `pair_config.md`와 별도 파일, 기존 파서 무영향) 초기화/재사용 로직으로 교체; Phase 2에 "분리 저장소 레인 실행" 절 추가 — 기존 2-0.5/2-1/2-2/2-4 절차·프롬프트를 그대로 재사용하되 같은 단계의 레인들을 한 메시지에서 병렬 호출(별도 폴링 메커니즘 불필요, 여러 `Agent()` 호출을 한 메시지에 담아 전부 반환될 때까지 기다리는 기존 관례 그대로가 곧 barrier); Phase 3.5를 "파트너 연동(pair-init)" 통짜 절에서 `P-BARRIER`/`P-PAIR`(pair-init Phase 2부터 호출 — 정보수집·자동생성 서브스텝은 이미 레인에서 끝났으므로 스킵)/`P-REFRESH`(드리프트 1회 재확인) 3단계로 재편, Phase 4 도입부의 옛 조기발사 교차 참조 정정. `skills/pair-init/SKILL.md`의 "파트너 하네스 자동 생성" 서브스텝(Phase 1-A/1-B)에 "harness-init 주도 흐름에서는 이제 이 서브스텝을 호출하지 않는다(harness-init 자체가 레인을 지휘) — pair-init 단독 호출 시에만 쓰인다" 안내 추가, 그 외 Phase(0, 2~6)는 무변경(P-PAIR/P-REFRESH가 그대로 재사용). `agents/validator.md`는 검토 결과 "T-V" 같은 task-id 표시 관례를 이 파일 자체가 갖고 있지 않아(표시 이름은 순전히 harness-init.md의 Agent `description` 필드 관례) 변경 대상이 아님을 확인, 무변경. 이번 변경은 전부 SKILL.md 프로즈/오케스트레이션 로직이라 2026-08-12와 동일하게 실제 분리 저장소 2개로 harness-init을 풀 실행하는 검증(비용 큼)은 이번 턴엔 생략하고 문서 정합성 재검토로 대체. 플러그인 버전 0.11.2 → 0.12.0 | skills/harness-init/SKILL.md, skills/pair-init/SKILL.md, .claude-plugin/plugin.json, CLAUDE.md | 사용자가 harness-init Phase 1 작업 그래프 설계안(T-I 분리, 분리 저장소 대칭 2-레인+P-BARRIER/P-PAIR/P-REFRESH, T-V 명칭)을 제시하며 "지금꺼랑 비교해서 어떤게 나아?" 질문 → 비교 설명 후 4가지 세부 결정을 확인받고 진행 |
| 2026-08-13 | wiki 결함 2건 수정. **(1) API/외부 시스템 설명 부재**: 사용자가 "api-endpoints/external-systems 페이지에 기능·역할 설명이 없어 위키로서 쓸모없다, 그 설명이 architecture 안에만 다 들어가 있는 것 같다"고 지적 — 조사 결과 그 설명(analyzer가 이미 파악한 내용)은 `01_analyzer_report.md`(→architecture.md) 프로즈에만 있고, `docs/index-schema/api_contract.schema.json`(`endpoints[]`/`consumers[]`)·`external_io.schema.json`(`communications[]`)에는 애초에 "설명" 필드 자체가 없어 `wiki_content.py`가 렌더링할 데이터가 없었음(스키마 확인으로 근본 원인 확정). call_graph의 모호 관계 보강에 이미 쓰던 패턴(analyzer가 원본 파일을 직접 안 고치고 `_ai_patch.json`에 `add_edge` 오퍼레이션만 제출 → 인덱서가 검증 후 병합, incremental 재인덱싱 시 직접 수정분이 사라지는 걸 막기 위함)을 그대로 확장 — `_ai_patch.json`에 `set_endpoint_description`/`set_communication_description`(`{id, description}`) 오퍼레이션 2종 추가. `build-index.mjs`의 `applyAiPatch()`를 오퍼레이션 종류별 버킷 분리 디스패치로 리팩터(신규 `mergeDescriptionPatch()`) — `add_edge` 버킷은 기존 로직 100% 무변경으로 `call_graph.json`에, 두 신규 버킷은 각각 `api_contract.json`/`external_io.json`에 적용, 미지원 오퍼레이션은 여전히 `unsupported_op`로 집계(기존 4개 patch 테스트 무수정 통과로 회귀 없음 확인). 두 스키마 파일에 선택적 `description` 필드 추가, `agents/analyzer.md` Step 11·15.5에 "기계 인덱스 있을 때는 패치로 보강, 없을 때(폴백)는 처음부터 필드 포함" 지침 + Step 8의 `_ai_patch.json` 예시에 두 오퍼레이션 추가, Phase C 표에 각주. `wiki_content.py`의 `_api_endpoints_table`/`_external_io_table`에 "설명" 컬럼 추가(없으면 빈 칸, 강제 아님). `docs/index-spec.md` 생성 주체 표·`external_io.json` 예시에도 반영. **(2) 콜 그래프 노드 상한 제거**: 사용자가 지난 세션에 추가한 "2000개 초과 시 나머지 숨김" 상한을 명시적으로 없애달라고 요청 — 물리엔진/엣지스무딩 기본값 자동 조정(데이터 손실 없는 성능 튜닝)은 그대로 두고, `wiki_generator.py`의 `MAX_RENDER_NODES` 절단 로직·`graph_capped` 배너·리포트 문단만 제거. 실제 실행 검증 — 더미 프로젝트(REST 컨트롤러+RestTemplate)로 `applyAiPatch()`→`wiki_generator.py` 전체 파이프라인을 돌려 api-endpoints.md/external-systems.md에 설명 컬럼이 실제로 채워짐을 확인, 2500노드 합성 그래프로 재실행해 전체가 잘림 없이 다 나오는 것도 확인. `node agents/lib/tests/run.js` 24개+신규 3개(설명 반영/unknown_id 거부/add_edge와 설명 보강 혼합 무오염) 전부 통과. 플러그인 버전 0.12.0 → 0.13.0 | docs/index-schema/api_contract.schema.json, docs/index-schema/external_io.schema.json, agents/lib/build-index.mjs, agents/lib/tests/build-index.test.mjs, agents/analyzer.md, agents/lib/wiki_content.py, docs/index-spec.md, agents/lib/wiki_generator.py, .claude-plugin/plugin.json, CLAUDE.md | 사용자가 "위키에 api endpoints/external system에 기능·역할 설명이 없어 위키로써 역할이 불가능하다, 이거 포함해줘. 그리고 콜그래프에 2천개만 보이고 나머지 안보이게 하는 것도 없애야 된다"고 요청 |
| 2026-08-13 | wiki에 "도메인 개요"(domain.md) 페이지 신설. 사용자가 "초기화하면 도메인 지식이나 시스템 전체 워크플로우/업무 플로우를 알려주는 메뉴도 있으면 좋겠다"고 제안 → 처음엔 완료 보고 메시지에 요약을 끼워넣는 안을 제시했으나, 사용자가 "wiki에서 볼 방법이 없길래 말하는 것"이라고 정정 — 실제 문제는 채팅 보고가 아니라 wiki 자체였음. 조사 결과 `architecture.md`(=`01_analyzer_report.md` 전체 그대로, 2026-07-15 결정)에 도메인/업무 흐름 내용이 이미 있긴 하지만 트랜잭션·OWASP·데드코드 등 기술적 세부사항과 뒤섞여 있고, `docsify_convert.py`의 사이드바 `PAGE_META`에는 "아키텍처" 하나만 있어 별도로 찾아 들어갈 방법이 없었다(같은 파일의 주석에 "workflows.md는 업무 워크플로우가 아니다"라는 인식은 이미 있었으나 그 실제 페이지는 만들어진 적이 없었음). `agents/analyzer.md`의 리포트 템플릿이 항상 고정된 `## A. 프로젝트 기본 정보`/`아키텍처 레이어`/`요청 흐름`/`코드 컨벤션`/`데이터 접근 패턴`/`클라이언트 자원`/`빌드 실행 명령` 순서를 쓰고 `## B.`(비동기·인증) 이후는 더 기술적인 내용이라는 걸 확인 — 이 고정 헤딩 경계를 이용해 `wiki_content.py`에 `_extract_domain_sections()`/`build_domain_overview()`(신규, zero-LLM 정규식 분리, architecture.md와 동일한 파트너 병합 파라미터 지원)를 추가했다. `architecture.md`는 전체 리포트라는 기존 성격을 그대로 유지(내용 제거 없음, domain.md와 일부 중복은 CLAUDE.md의 `request_flow` 필드처럼 이미 있던 패턴). `wiki_generator.py`에 `domain.md` 생성 단계 추가(Home.md 다음, architecture.md 이전 — 이후 단계 번호 주석 전부 +1 조정) + 파트너 병합 시 사이드바 앵커 노출 목록에도 추가, `docsify_convert.py`의 `PAGE_META`에 `domain`을 `architecture`보다 앞에 등록해 사이드바 "시스템 개요" 섹션 최상단에 노출되도록 함. 더미 analyzer report(법인카드 비용처리 시스템 예시, `## A.`/`## B.` 혼합)로 실제 생성해 domain.md엔 `## A.` 7개 섹션만, architecture.md엔 전체(`## B.`·탐지신뢰도 포함) 12개 섹션이 그대로 남는 것을 확인, 사이드바에 "도메인 개요"가 "아키텍처"보다 먼저 노출되는 것도 확인. `node agents/lib/tests/run.js` 27개 무회귀. 플러그인 버전 0.13.0 → 0.13.1 | agents/lib/wiki_content.py, agents/lib/wiki_generator.py, agents/lib/docsify_convert.py, .claude-plugin/plugin.json, CLAUDE.md | 사용자가 "초기화 하면 분석했을때 도메인 지식이나 이 시스템의 전체적인 워크플로우라던지 업무 플로우를 알려주는 메뉴도 있으면 좋을것 같아" → (완료보고 안 제시 후) "wiki에서 볼방법이 없길래 말하는거야"로 정정 |
| 2026-08-14 | 산출물 폴더 재구성 — 사용자가 "산출물 폴더가 너무 중구난방이다, 어떻게 모으는 게 좋을까?"라고 질문. 백그라운드 조사 에이전트로 harness가 대상 프로젝트에 쓰는 모든 파일/폴더 인벤토리 + 이동 위험도(SAFE/RISKY/DANGEROUS)를 먼저 조사한 뒤 3단계로 범위를 좁혀 확인받았다 — **절대 손대지 않은 것**: `_workspace/`·`.claude/`·`wiki/` 등 최상위 폴더명 자체는 `build-index.mjs`의 `EXCLUDED_DIRS`에 하드코딩돼 있어 이름을 바꾸면 인덱서가 하네스 자신의 산출물을 소스로 재인덱싱하게 되고, `_workspace/index/api_contract.json` 경로는 이미 배포된 프로젝트들의 `pair_config.md`에 절대경로로 박혀 있어(`build-index.mjs:1210` 주석) 건드리면 기존 크로스리포 연동이 조용히 깨진다 — 그래서 `index/`·`00~06_*.md`·`pair_config.md`·`ai-budget.json`·`validator_mechanical.json`·`validator_schema.json` 등 핵심 파이프라인 파일은 전부 그대로 유지. **실제로 옮긴 것 2가지**: (1) `wiki/`(프로젝트 루트) → `_workspace/wiki/` — "위키도 결국 분석 결과에서 나오는 재생성 가능한 산출물"이라는 근거로 다른 harness 산출물과 같은 `.gitignore` 권장 루트 아래로 통일. `build-index.mjs`의 `EXCLUDED_DIRS`에서 `"wiki"`/`"wiki_prev"` 항목 제거(이제 `_workspace` 제외 규칙이 중첩 경로까지 자동으로 커버 — 부수 효과로 대상 프로젝트가 우연히 `wiki`라는 이름의 자기 소스 폴더를 쓸 때 잘못 제외되던 문제도 해소), `agents/lib/wikihub_db/publish.py`의 `--wiki-dir` 기본값도 함께 갱신(publish-wiki 스킬이 이 기본값에 의존해 명시적으로 인자를 안 넘기고 있었음), `generate-wiki`/`publish-wiki`/`wiki-hub`/`cross-repo-scaffold`/`cross-repo-modify`/`harness-clean` 등 SKILL.md 전반의 경로 표기 갱신. (2) `_workspace/` 바로 밑에 흩어져 있던 1회성 작업 리포트 9종(`impact_<slug>.md`/`safety_<slug>.md`/`trace_<slug>.md`/`found_<slug>.md`/`sql_review_<slug>.md`/`docs_sync_<slug>.md`/`api_drift_report.md`/`cross_scaffold_backend.md`/`cross_scaffold_frontend.md`/`cross_modify_partner.md`)를 `_workspace/reports/`로 이동 — 각자 자기 스킬/에이전트 파일에서만 참조되고(`agents/impact-analyzer.md`·`change-safety.md`·`logic-tracer.md`·`feature-finder.md`·`sql-reviewer.md`·`doc-syncer.md`·`api-bridge.md`, `skills/analyze-impact`·`safe-modify`·`trace-logic`·`find-feature`·`review-sql`·`cross-repo-scaffold`·`cross-repo-modify`·`pair-init`·`harness-init/SKILL.md` 등 약 15개 파일) 다른 배포된 설정에 절대경로로 박혀있지 않아 안전했다. **덤**: `harness-clean/SKILL.md`가 2026-08-13에 이미 로컬 배포를 끊은 6개 스킬 파일(analyze-impact.md 등)을 여전히 삭제 대상으로 나열하던 문서 드리프트를 정정하고, 대신 실제로 배포되는 `trace-logic.md`/`find-feature.md`/`cross-repo-scaffold.md`/`cross-repo-modify.md`/`ito-guide.md`를 목록에 추가. 실제 실행 검증 — 더미 프로젝트로 `wiki_generator.py --wiki-dir _workspace/wiki` 실행해 정상 생성 확인, 이어서 `build-index.mjs`로 재인덱싱해 `_workspace/wiki/*.html`이 소스로 재인덱싱되지 않는 것(파일 스캔 수 1개, `src/hello.ts`만)까지 확인. `node agents/lib/tests/run.js` 27개 무회귀. 플러그인 버전 0.13.1 → 0.14.0 | agents/lib/build-index.mjs, agents/lib/wikihub_db/publish.py, skills/generate-wiki/SKILL.md, skills/publish-wiki/SKILL.md, skills/wiki-hub/SKILL.md, skills/harness-clean/SKILL.md, skills/cross-repo-scaffold/SKILL.md, skills/cross-repo-modify/SKILL.md, skills/analyze-impact/SKILL.md, skills/safe-modify/SKILL.md, skills/trace-logic/SKILL.md, skills/find-feature/SKILL.md, skills/review-sql/SKILL.md, skills/pair-init/SKILL.md, skills/harness-init/SKILL.md, agents/impact-analyzer.md, agents/change-safety.md, agents/test-generator.md, agents/logic-tracer.md, agents/feature-finder.md, agents/sql-reviewer.md, agents/doc-syncer.md, agents/api-bridge.md, docs/wiki-hub.md, docs/user-guide.md, README.md, .claude-plugin/plugin.json, CLAUDE.md | 사용자가 "산출물 폴더정리가 필요할것 같아. 너무 중구난방이야. 어떻게 모으는게 좋을까?"라고 질문 → 조사·제안·범위 확인 후 "둘 다 진행해줘"(직전 wiki 기능 커밋·푸시 + 이 폴더정리) 요청으로 진행 |
| 2026-08-14 | `_workspace/.index-cache/`(파일별 해시 캐시) 완전 폐지. 앞선 폴더정리 세션에서 사용자가 "index cache 생기는 것도 없애줘, 필요없어"라고 요청 — 다만 이 캐시가 단순 부산물이 아니라 `--mode incremental`("인덱스만 갱신해줘")이 내용 안 바뀐 파일의 재분석을 건너뛰게 해주는 유일한 메커니즘이라는 점을 먼저 알리고("레거시 4,883파일 전체 스캔 37초" 사례 인용) 확인받았다 — 사용자가 "37초 정도면 뭐, 캐싱 기능 포기해도 괜찮다"고 승인. `agents/lib/build-index.mjs`의 `buildIndex()`에서 `.index-cache/files/*.json` 캐시 읽기·쓰기·정리 로직(파일 내용 sha256 해시 비교) 전체를 제거하고 모든 파일을 항상 `analyzeFile()`로 재분석하도록 단순화(`analyzed = files.length`, `reused = 0` 고정) — `preservePatch`(incremental 시 기존 `_ai_patch.json` 보존 여부)는 캐시와 무관한 별개 로직이라 그대로 유지, `mode` 값 자체도 `_meta.json` 기록·`preservePatch` 판단용으로 계속 쓰인다. 캐시 전용이던 `sha256()`/`createHash` import도 다른 용도가 전혀 없어 함께 제거. `agents/lib/wikihub_db/publish.py`의 `EXCLUDED_WORKSPACE_DIRS`(`.index-cache` 예외 처리용, 이제 생성 자체가 안 되니 무의미)도 `EXCLUDED_DIRS`와 동일해져 중복 상수를 없애고 하나로 합침. 기존 테스트 1개(`second.analyzed === 0` 가정)가 새 동작과 어긋나 실패하는 것을 확인 후 `second.analyzed === second.files`/`second.reused === 0`으로 계약 갱신(캐시 폐지를 실제로 검증하는 회귀 테스트로 전환). 더미 프로젝트로 `init`→`incremental` 연속 실행해 `_workspace/`에 `.index-cache/`가 전혀 생기지 않고 두 모드 모두 정상 동작하는 것 실제 확인. `node agents/lib/tests/run.js` 27개 무회귀. 플러그인 버전 0.14.0 → 0.14.1 | agents/lib/build-index.mjs, agents/lib/tests/build-index.test.mjs, agents/lib/wikihub_db/publish.py, skills/harness-init/SKILL.md, .claude-plugin/plugin.json, CLAUDE.md | 사용자가 "index cache 생기는거도 없애줘. 필요없어"라고 요청 → incremental 모드의 유일한 가속 수단이라는 트레이드오프를 알린 뒤 "캐싱기능 포기해도 괜찮을것 같아. 37초정도면 뭐."라는 승인을 받고 진행 |
